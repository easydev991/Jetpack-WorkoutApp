#!/bin/bash

echo "Updating versions in README.md..."
./gradlew updateReadmeVersions --no-configuration-cache --quiet

if [ $? -eq 0 ]; then
    echo "✓ Versions updated successfully"
    git add README.md
else
    echo "✗ Failed to update versions"
    exit 1
fi
