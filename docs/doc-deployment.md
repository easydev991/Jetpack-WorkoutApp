# Релизный процесс

- [Секреты для подписи](#секреты-для-подписи)
- [Создание сборки](#создание-сборки)
  - [Релизный AAB (для публикации в магазине)](#релизный-aab-для-публикации-в-магазине)
  - [Релизный APK (для распространения вручную)](#релизный-apk-для-распространения-вручную)
- [Управление версией](#управление-версией)
  - [Версия приложения (VERSION_NAME)](#версия-приложения-version_name)
  - [Номер сборки (VERSION_CODE)](#номер-сборки-version_code)
- [Публикация](#публикация)
- [Скриншоты](#скриншоты)
  - [Генерация скриншотов](#генерация-скриншотов)
  - [Обновление README.md](#обновление-readmemd)
- [Подготовка Room Database к релизу](#подготовка-room-database-к-релизу)

## Секреты для подписи

Секреты для подписи хранятся в отдельном репозитории: `../android-secrets/swparks`

Команды `make release` и `make apk` автоматически загружают их в папку `.secrets/` перед сборкой.

## Создание сборки

### Релизный AAB (для публикации в магазине)

Для создания подписанной AAB-сборки выполните:

```bash
make release
```

Команда автоматически:

1. Увеличивает номер сборки (`VERSION_CODE`) на 1
2. Создает подписанную AAB-сборку: `swparks{VERSION_CODE}.aab` (например, `swparks1.aab`, `swparks2.aab`)
3. Отображает версию и номер сборки

### Релизный APK (для распространения вручную)

Для создания подписанного APK есть команда:

```bash
make apk
```

Команда автоматически:

1. Создает подписанный APK-файл: `swparks{VERSION_CODE}.apk` (например, `swparks1.apk`, `swparks2.apk`)
2. Отображает версию и номер сборки
3. **Не увеличивает** номер сборки (`VERSION_CODE`)

## Управление версией

### Версия приложения (VERSION_NAME)

Отображается в магазине приложений. Изменяется вручную в `gradle.properties`:

```properties
VERSION_NAME=1.0
```

Примеры версий:

- `1.0` - первый релиз
- `1.1` - минорное обновление с новыми функциями
- `2.0` - мажорное обновление с изменениями в функционале

### Номер сборки (VERSION_CODE)

Автоматически увеличивается при каждом `make release`. Изменения вручную не требуется.

Формат: целое число, монотонно возрастающее.

```
VERSION_CODE=1
```

Пример:

```
VERSION_NAME=1.0, VERSION_CODE=1 → make release → VERSION_CODE=2 → AAB: swparks2.aab (1.0 build 2)
VERSION_NAME=1.0, VERSION_CODE=2 → make release → VERSION_CODE=3 → AAB: swparks3.aab (1.0 build 3)
VERSION_NAME=1.1, VERSION_CODE=3 → make release → VERSION_CODE=4 → AAB: swparks4.aab (1.1 build 4)
```

**Примечание:** Номер сборки (VERSION_CODE) никогда не сбрасывается при повышении версии приложения, поэтому он всегда уникален и монотонно возрастает.

## Публикация

- Для публикации в магазине приложений используется файл `swparks.aab`
- Для ручного распространения используется файл `swparks.apk`

## Скриншоты

### Генерация скриншотов

Для генерации скриншотов выполните:

```bash
make screenshots
```

Эта команда:

1. Собирает APK для скриншотов
2. Запускает fastlane для захвата скриншотов на эмуляторе
3. Генерирует скриншоты для локалей `ru-RU` и `en-US` в папку `fastlane/metadata/android/`

**Важно:** Для успешного создания скриншотов необходимо чтобы был запущен эмулятор с соответствующими требованиями для RuStore:

- Соотношение сторон экрана: 9 x 16
- Рекомендуемое разрешение: 1080 х 1920

### Обновление README.md

После генерации скриншотов нужно выполнить команду для обновления таблицы скриншотов в README.md:

```bash
make update_readme
```

Эта команда автоматически:

1. Находит актуальные файлы скриншотов по шаблону `{номер}-{описание}_{временнаяМетка}.png`
2. Заменяет HTML-комментарии в README.md на реальные теги `<img>` с путями к файлам
3. Использует скриншоты только из локали `ru-RU`

Порядок работы:

```bash
# 1. Сгенерировать скриншоты
make screenshots

# 2. Обновить таблицу в README.md
make update_readme

# 3. Зафиксировать изменения в гите
```

## Подготовка Room Database к релизу

⚠️ **ВАЖНО:** Перед релизом необходимо удалить `fallbackToDestructiveMigration()` и добавить миграции для сохранения данных пользователей!

### На этапе разработки

Используется `fallbackToDestructiveMigration()` для удобства:

```kotlin
val database = Room.databaseBuilder(
    context.applicationContext,
    SWDatabase::class.java,
    "sw_database"
).fallbackToDestructiveMigration()  // ❌ УДАЛИТЬ ПЕРЕД РЕЛИЗОМ
  .build()
```

**Что делает этот метод:**
- Удаляет **ВСЕ данные** в БД при изменении схемы
- Пересоздает БД с нуля
- Удобно для разработки, но **НЕБЕЗОПАСНО для продакшена**

### Перед релизом

**Удалить** `fallbackToDestructiveMigration()` и добавить миграции:

```kotlin
val MIGRATION_1_2 = object : Migration(1, 2) {
    override fun migrate(database: SupportSQLiteDatabase) {
        // SQL для изменения структуры таблицы
        database.execSQL("ALTER TABLE users ADD COLUMN newColumn TEXT")
    }
}

val database = Room.databaseBuilder(
    context.applicationContext,
    SWDatabase::class.java,
    "sw_database"
).addMigrations(MIGRATION_1_2)  // ✅ ДОБАВИТЬ МИГРАЦИИ
  .build()
```

**Почему это обязательно:**
- Сохранение данных пользователя при обновлении приложения
- Предотвращение потери доверия пользователей
- Корректный UX при обновлении версии

### Если первая версия приложения

Если Room Database используется впервые (нет существующей версии БД), то:

```kotlin
@Database(
    entities = [UserEntity::class],
    version = 1,  // Первая версия
    exportSchema = false
)
abstract class SWDatabase : RoomDatabase()
```

- `version = 1` - первая версия БД
- Миграции не нужны
- `fallbackToDestructiveMigration()` можно использовать для разработки

### Чек-лист перед релизом

- [ ] Удален `fallbackToDestructiveMigration()` из кода
- [ ] Добавлены миграции для всех изменений схемы
- [ ] Протестирована миграция на тестовом устройстве (с реальными данными)
- [ ] Увеличен `VERSION_NAME` при необходимости
- [ ] Сгенерированы и обновлены скриншоты
- [ ] Выполнен `make release` и создан AAB-файл
