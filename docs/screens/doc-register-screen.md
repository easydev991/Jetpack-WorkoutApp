# Регистрация нового пользователя

## Функционал

Экран регистрации `RegisterUserScreen` открывается из профиля и позволяет создать аккаунт.

**Поля формы:**
- Логин, Email, Пароль
- Имя, Пол, Дата рождения (по умолчанию сегодняшняя)
- Страна и Город (через nested sheet)
- Принятие пользовательского соглашения

**Валидация:**
- Пароль: минимум 6 символов
- Email: корректный формат
- Дата рождения: не в будущем (возраст не проверяется)

**Подсказки:**
- Логин: латиница, цифры, подчёркивание (без валидации)

**Навигация:**
- Выбор страны/города через отдельные экраны в sheet
- Sheet нельзя закрыть свайпом
- После выбора города автоматически подставляется страна

**API и данные:**
- Регистрация через `SWApi.register()` с `@FormUrlEncoded`
- Возвращает модель `User`, которая сохраняется в базу
- Токен сохраняется, профиль загружается без лишнего запроса

**Обработка ошибок:**
- Ошибки отображаются в Snackbar внутри sheet

---

## Реализованные файлы

### UI Models

| Файл                          | Описание                     |
|-------------------------------|------------------------------|
| `ui/model/RegisterForm.kt`    | Data class формы регистрации |
| `ui/state/RegisterUiState.kt` | Sealed class состояний UI    |
| `ui/state/RegisterEvent.kt`   | События UI                   |

### ViewModel

| Файл                                 | Описание             |
|--------------------------------------|----------------------|
| `ui/viewmodel/IRegisterViewModel.kt` | Интерфейс ViewModel  |
| `ui/viewmodel/RegisterViewModel.kt`  | Реализация ViewModel |

### Screens

| Файл                                             | Описание                   |
|--------------------------------------------------|----------------------------|
| `ui/screens/auth/RegisterUserScreen.kt`          | Основной экран регистрации |
| `ui/screens/auth/RegisterSheetHost.kt`           | Sheet-хост для регистрации |
| `ui/screens/auth/RegisterSelectCountryScreen.kt` | Экран выбора страны        |
| `ui/screens/auth/RegisterSelectCityScreen.kt`    | Экран выбора города        |

### Tests

| Файл                                                          | Описание                         |
|---------------------------------------------------------------|----------------------------------|
| `test/java/com/swparks/ui/viewmodel/RegisterViewModelTest.kt` | Unit-тесты ViewModel (668 строк) |

**Отсутствуют:**
- UI тесты для `RegisterUserScreen` (автоматические тесты интерфейса)

---

## Тестирование

### Автоматические тесты

- **Unit-тесты:** ✅ `RegisterViewModelTest.kt` (668 строк)
- **UI тесты:** ❌ Отсутствуют (RegisterUserScreenTest.kt не создан)

### Ручное тестирование (требуется устройство)

- [x] Открытие экрана регистрации из профиля
- [x] Валидация пароля (минимум 6 символов)
- [x] Валидация email
- [x] Выбор пола
- [x] Выбор даты рождения
- [x] Выбор страны и города
- [x] Принятие пользовательского соглашения
- [x] Успешная регистрация
- [x] Обработка ошибок сети

---

## Технические детали

**Компоненты:** `SWTextField`, `DateTimePicker`, `PolicyToggle`, `LoadingOverlayView`

**API:** `SWApi.register()` с `@FormUrlEncoded`

**Навигация:** Вызов из `ProfileScreen` → `RegisterSheetHost`
