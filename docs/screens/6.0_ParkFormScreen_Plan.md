# План: ParkFormScreen - Создание/редактирование площадки

## Обзор

**Задача:** Реализовать экран для создания и редактирования площадок (парков) с двумя режимами работы.

**Референс:**
- Android: `EventFormScreen.kt` (аналогичная форма)
- iOS: `SwiftUI-WorkoutApp/Screens/Parks/ParkFormScreen.swift` + `SWModels/ParkForm.swift` + `ParkFormTests.swift`

**Статус:** Этапы 1-5 (инфраструктура) завершены. Stage 5.4 в процессе. Этапы 6.1/6.3 pending.

**Ключевое отличие от EventFormScreen:**
- Два селектора **типа** и **размера** площадки (RadioButton по аналогии с `RegisterGenderSection`)
- Логика `canSave` и валидация полностью совпадают с iOS-референсом

## Основные компоненты

### iOS ParkForm (ParkGrade/ParkSize)

| Поле | Тип | Описание |
|------|-----|----------|
| `address` | String | Адрес площадки |
| `latitude` | String | Широта |
| `longitude` | String | Долгота |
| `cityId` | Int? | ID города |
| `typeId` | Int | Тип площадки (ParkGrade) |
| `sizeId` | Int | Размер площадки (ParkSize) |
| `photosCount` | Int | Количество существующих фото |
| `newMediaFiles` | [MediaFile] | Новые фото |

### Android ParkType / ParkSize

| ParkType | rawValue | R.string |
|----------|----------|----------|
| SOVIET | 1 | `soviet_park` |
| MODERN | 2 | `modern_park` |
| COLLARS | 3 | `collars_park` |
| LEGENDARY | 6 | `legendary_park` |

| ParkSize | rawValue | R.string |
|----------|----------|----------|
| SMALL | 1 | `small_park` |
| MEDIUM | 2 | `medium_park` |
| LARGE | 3 | `large_park` |

---

## Этап 1 (TDD): Модель ParkForm ✅

- [x] **RED/GREEN**: ParkForm unit-тесты (create/update ready/not-ready, `equals`, `imagesLimit`) — 35 тестов
- [x] **GREEN**: Использована существующая `ParkForm.kt` (без дублирования)
- [x] **GREEN**: Добавлены `photosCount`, `newMediaFiles`, `imagesLimit`, `gradeStringRes`/`sizeStringRes`
- [x] **GREEN**: Фабрики из `Park` (edit) и create-аргументов (`address`, `latitude`, `longitude`, `cityId`)
- [x] **GREEN**: `isReadyToCreate` / `isReadyToUpdate` — строгий iOS-паритет
- [x] **GREEN**: String resources: `new_park_title`, `park_type`, `park_size`, `address`, confirm-close диалог

---

## Этап 2 (TDD): Data Layer и контракт сохранения ✅

- [x] **RED/GREEN**: unit-тесты save- boundary (create/edit с фото и без)
- [x] **GREEN**: Использован `SWRepository.savePark(id, form, photos)`
- [x] **GREEN**: UI `Uri` -> `ByteArray` -> `savePark(..., photos)` mapping
- [x] **GREEN**: API-паритет multipart: `address`, `latitude`, `longitude`, `city_id`, `type_id`, `class_id`, `photos`

---

## Этап 3 (TDD): ViewModel ✅

- [x] **RED/GREEN**: `IParkFormViewModel` interface + `ParkFormViewModel` implementation
- [x] **GREEN**: State (`form`, `isSaving`, `hasChanges`, `canSave`, `mode`), Actions, Events
- [x] **RED/GREEN**: 33 unit-теста — create/edit init, canSave, photo handling, save success/error
- [x] **GREEN**: `canSave` iOS-parity (create mode без `hasChanges`)
- [x] **REFACTOR**: упрощён reducer/action-handlers

---

## Этап 4 (TDD): UI Layer (Compose) ✅

- [x] **GREEN**: `ParkFormScreen.kt` (407 строк), `FakeParkFormViewModel.kt`, `ParkFormScreenTest.kt` (17 тестов)
- [x] **GREEN**: `ParkTypeRadioButtons` / `ParkSizeRadioButtons` — аналог `GenderRadioButtons`
- [x] **GREEN**: `ParkTypeSelectorSection` / `ParkSizeSelectorSection` с `FormCardContainer`
- [x] **GREEN**: `ParkFormNavigationAction`: `Back`, `BackWithSavedPark(Park)`
- [x] **GREEN**: UI-тесты — все поля, radio buttons, навигация back, Save disabled/enabled

---

## Этап 5 (TDD): Навигация ✅ (Infrastructure) / ⚠ (Location integration)

### 5.1-5.2 Routes + NewParkDraft ✅

- [x] `Screen.CreatePark` / `Screen.EditPark` composables в `RootScreen`
- [x] `EditParkNavArgs` / `CreateParkNavArgs` + ViewModels + `consume*Args()`
- [x] `navigateToEditPark()` / `navigateToCreatePark()` в `ParkNavigationCoordinator`
- [x] `NewParkDraft` model + 18 unit-тестов, `shouldRequestLocation` / `shouldPerformGeocode`
- [x] `ParkDetail -> EditPark` event + coordinator ✅; refresh после сохранения ✅

### 5.3 Геолокация: FAB + Permissions ✅

- [x] `ACCESS_FINE_LOCATION`/`ACCESS_COARSE_LOCATION` в AndroidManifest.xml
- [x] `CreateParkFab` интегрирован с `LocationPermissionState` ( ParksRootScreen.kt)
- [x] `LocationPermissionAlertDialog` composable + strings (`values/` + `values-ru/`)
- [x] UI-тесты: `ParksRootScreenTest` + `LocationPermissionAlertDialogTest` (7 сценариев)
- ⚠️ actual location retrieval + geocoding → Stage 5.4

---

### 5.4 Location retrieval + Geocoding (TDD, iOS-parity) 🔄

**Обзор:** create-flow — Шаг 1: получить координаты → `NewParkDraft` → форма; Шаг 2: geocoding в форме (если `shouldPerformGeocode`)

**Типы данных:**
- `NewParkDraft.cityId: Int?` (сервер ожидает Int), `City.id: String`, `User.cityID: Int?`
- Конвертация String→Int: `.toIntOrNull()`

**Порядок:** 5.4.1 → 5.4.2 → 5.4.3 → 5.4.5 → 5.4.4 → 5.4.4b → 5.4.4c → 5.4.6/7/8

**Refactor (после 5.4.4c):**
- [x] `ICreateParkLocationHandler` интерфейс → `DefaultCreateParkLocationHandler` (чистая логика без Android runtime)
- [x] `LocationPermissionState` делегирует, `AppContainer` содержит `createParkLocationHandler`
- [x] Регрессия: ktlint ✅, testDebug ✅ (120 files), assembleDebug ✅
- [x] **`5.4.4c`** ✅: `invoke()` → `Result<NewParkDraft>`; `onFailure` → `handleError(AppError.Generic(...))` + `Result.failure()`; навигация только при `isSuccess`

#### 5.4.1 Location/Geocoding Contracts ✅

- [x] `LocationService` (`getCurrentLocation(): Result<LocationCoordinates>`)
- [x] `GeocodingService` (`reverseGeocode(lat, lng): Result<GeocodingResult>`)
- [x] `LocationCoordinates`, `GeocodingResult` data classes

#### 5.4.2 Data Layer Implementation ✅

- [x] `LocationServiceImpl` — `FusedLocationProviderClient`, таймаут 10 сек, fallback `LocationManager`
- [x] `GeocodingServiceImpl` — `Geocoder`, address из placemark fields, `IOException`/empty handling
- [x] `play-services-location` dependency added
- [x] Unit-тесты для обоих impl

#### 5.4.3 City Lookup ⚠

- [ ] написать unit-тесты (`invoke_whenLocalityMatches_returnsCityId`, `invoke_whenLocalityNull_returnsNull`, `invoke_whenCityIdIsNotInt_returnsNull`, `invoke_whenNoMatch_returnsNull`)
- [ ] реализовать `FindCityByCoordinatesUseCase` — `locality` → `CountriesRepository.getAllCities()` (case-insensitive, first match) → `city.id.toIntOrNull()`
- [ ] fallback: `currentUser.cityID` при `null`

#### 5.4.4 ParksRootScreen Integration ⚠

- [ ] **RED**: androidTest — `onFabClicked_whenPermissionGranted_fetchesLocationAndNavigates`, `onFabClicked_whenLocationFails_navigatesWithEmptyDraft`
- [ ] **GREEN**: `LocationPermissionState` + `LocationService`, fetch → build draft → navigate
- [ ] **GREEN**: warning notification при location failure
- iOS parity: geocoding в `ParkForm` (не здесь)

#### 5.4.4b ParkForm Geocoding ⚠

- [ ] **RED**: unit-тесты geocoding веток create-mode
- [ ] **GREEN**: `performGeocodingForNewPark` → `GeocodingService` → `FindCityByCoordinatesUseCase`
- [ ] **GREEN**: fallback `currentUser.cityID`; если недоступен → `cityId = null` (ручной ввод)
- [ ] **GREEN**: user notification через `UserNotifier`

#### 5.4.5 DI ⚠

- [ ] `LocationServiceImpl`, `GeocodingServiceImpl`, `FindCityByCoordinatesUseCase` в `AppContainer`

#### 5.4.6 String Resources ✅

| Ключ | Назначение | iOS-паритет |
|------|------------|-------------|
| `location_fetch_failed` | Координаты не получены → error через `handleError` | нет iOS-строки |
| `geocoding_failed` | Координаты получены, адрес не определён → ручной ввод | `GeocodingError.FailedToCreateAddress` |

- [x] Строки добавлены в `values/` и `values-ru/`
- [x] **УДАЛЕНО**: `city_not_found_use_manual` — нет iOS-паритета

#### 5.4.7 Integration Tests ⚠

- [ ] **RED/GREEN**: ParksRootScreen permission/location androidTest; ParkFormViewModel geocoding fallback unit-tests

#### 5.4.8 Type Safety ⚠

- [ ] `FindCityByCoordinatesUseCase` выполняет `city.id.toIntOrNull()`

---

## Этап 6 (TDD): Интеграция с существующими экранами ⚠

### 6.1 ParksRootScreen (Create entrypoint) ⚠

- `CreateParkFab` + `LocationPermissionState` интегрированы ✅
- location retrieval + geocoding → Stage 5.4
- [ ] **RED**: UI/Nav integration tests для create-flow (`ParksRootScreen`/`RootScreen`)
- [ ] **GREEN**: после 5.4 — `NewParkDraft` с координатами + geocoding в `ParkForm`

### 6.2 ParkDetailScreen (Edit flow) ✅

- [x] `NavigateToEditPark` event → `ParkDetailViewModel.onEditClick()` → coordinator → `RootScreen`
- [x] `updatedPark` через `savedStateHandle` → `ParkDetailViewModel.onParkUpdated(id)`
- [x] Unit-тесты: `onEditClick_thenEmitsNavigateToEditPark`, `onParkUpdated_whenCalled_thenReloadsPark`
- [ ] **RED**: UI/Nav integration tests для edit-flow из `ParkDetail`

### 6.3 Интеграционные тесты create-flow ⚠

- `RootScreen` использует runtime `AppContainer` → e2e нестабилен
- [ ] **RED**: ParksRootScreen permission UI-тесты; `ParkNavigationCoordinator` unit-tests
- [ ] **RED**: `RootScreen` integration: FAB → `CreatePark` → prefill из `CreateParkNavArgs`

---

## Критерии завершения

### Выполнено ✅

- [x] Этапы 1-4: ParkForm (35 тестов), ParkFormViewModel (33 теста), ParkFormScreen (17 тестов) — полный TDD
- [x] Этап 5 (Infrastructure): routes, nav args, `NewParkDraft` (18 тестов), permissions, FAB + LocationPermissionState
- [x] Этап 6.2 (Edit flow): `ParkDetail -> EditPark`, refresh после сохранения, unit-тесты
- [x] `make lint` ✅, `make test` ✅ (120 unit-test files)

### В процессе 🔄

- [ ] **Stage 5.4**: LocationService ✅, GeocodingService ✅, FindCityByCoordinatesUseCase ⚠, ParksRootScreen ⚠, ParkForm geocoding ⚠, DI ⚠
- [ ] Интеграционные тесты create-flow / edit-flow (UI/Nav)

---

## Заметки по реализации

- RadioButtons: паттерн из `GenderRadioButtons` (RegisterUserScreen.kt:473)
- FormCardContainer: группировка селекторов
- PickedImagesGrid, ImagePreviewDialog, LoadingOverlay, KeyboardAwareBottomBar: готовые компоненты

## Out of Scope

- Offline поддержка
- Дополнительная валидация координат
