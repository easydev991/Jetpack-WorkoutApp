# План: ParkFormScreen - Создание/редактирование площадки

## Обзор

**Задача:** Реализовать экран для создания и редактирования площадок (парков) с двумя режимами работы.

**Референс:**
- Android: `EventFormScreen.kt` (аналогичная форма)
- iOS: `SwiftUI-WorkoutApp/Screens/Parks/ParkFormScreen.swift` + `SWModels/ParkForm.swift` + `ParkFormTests.swift`

**Статус:** Этапы 1-4 завершены. Этапы 5 (инфраструктура) и 6.2 (edit-flow) завершены. Stage 5.3: геолокация интегрирована, тесты добавлены (2026-03-22). Stage 5.4.1/5.4.2: контракты + data-реализации location/geocoding c unit-тестами завершены (2026-03-22). Stage 5.4.3+ и Stage 6.1/6.3: в процессе.

**Ключевое отличие от EventFormScreen:**
- Вместо `ParkSelector` (один селектор площадки) -> два селектора **типа** и **размера** площадки
- Оба селектора реализованы как **RadioButton** (по аналогии с `RegisterGenderSection`)
- Логика `canSave` и валидация формы должны полностью совпадать с iOS-референсом

## TDD-стратегия (обязательно)

Для каждого этапа использовать цикл:
1. **RED**: сначала написать/обновить тесты и зафиксировать, что они падают на текущей реализации.
2. **GREEN**: добавить минимальную реализацию, чтобы тесты стали зелеными.
3. **REFACTOR**: улучшить код без изменения поведения и повторно прогнать тесты/lint.

Правило для этой задачи: **ни одного production-изменения без соответствующего теста, который был написан до реализации**.

---

## Ревью последних 6 коммитов (обновлено 2026-03-22)

Ниже зафиксированы обязательные доработки до завершения Stage 5/6.

### Blockers (обязательно исправить)

- [x] **P0**: скрыть BottomNavigation для CreatePark/EditPark. ✅ (2026-03-22)
  - [x] **GREEN**: добавлены `Screen.CreatePark` и `Screen.EditPark` в `BOTTOM_BAR_HIDDEN_BASE_ROUTES` ✅
- [x] **P0**: Infrastructure для create-flow с координатами. ✅ (2026-03-22)
  - [x] **GREEN**: создана модель `NewParkDraft` с полями `latitude`, `longitude`, `address`, `cityId`, `lastLocationRequestDate`
  - [x] **GREEN**: реализованы `shouldRequestLocation` и `shouldPerformGeocode` properties
  - [x] **GREEN**: добавлены unit-тесты для `NewParkDraft` (18 тестов в `NewParkDraftTest.kt`)
  - [x] **GREEN**: реализованы `CreateParkNavArgs` / `consumeCreateParkArgs()` в `ParkNavArgs.kt`
  - [x] **GREEN**: добавлен `CreateParkNavArgsViewModel` в `NavArgsViewModels.kt`
  - [x] **GREEN**: реализован `navigateToCreatePark(source, draft)` в `ParkNavigationCoordinator.kt`
  - [x] **GREEN**: добавлены `ACCESS_FINE_LOCATION` / `ACCESS_COARSE_LOCATION` в AndroidManifest.xml
  - [x] **GREEN (Stage 5.3)**: интегрирован `CreateParkFab` с location permissions ✅ (2026-03-22)
  - [ ] **TODO (Stage 5.4)**: реализовать actual location retrieval + geocoding (TDD-план добавлен)
  - [ ] **TODO (Stage 6.3)**: интеграционные тесты без сетевой зависимости
- [x] **P1**: довести edit-flow из `ParkDetail`. ✅ (2026-03-21)
  - [x] **RED/GREEN**: добавлен тест `onEditClick_thenEmitsNavigateToEditPark`, `ParkDetailEvent.NavigateToEditPark` добавлен.
  - [x] **GREEN**: `onEditClick()` в `ParkDetailViewModel` теперь emitит `NavigateToEditPark` событие.
  - [x] **GREEN**: `ParkDetailScreen` получает `onNavigateToEditPark` callback и обрабатывает событие.
  - [x] **GREEN**: `RootScreen` вызывает `navigateToEditPark()` через `ParkNavigationCoordinator`.
  - [x] **REFACTOR**: паттерн унифицирован с `EventDetailEvent.NavigateToEditEvent`.
- [x] **P1**: после сохранения из `EditPark` гарантированное обновление `ParkDetail`. ✅ (2026-03-21)
  - [x] **GREEN**: `EditPark` route в `RootScreen` передаёт сохранённый парк через `savedStateHandle["updatedPark"]`.
  - [x] **GREEN**: `LaunchedEffect` в ParkDetail route вызывает `parkDetailViewModel.onParkUpdated(updatedPark.id)`.
  - [x] **GREEN**: добавлен unit-тест `onParkUpdated_whenCalled_thenReloadsPark` в `ParkDetailViewModelTest.kt`.
- [x] **P1**: iOS-parity в `canSave` для create. ✅ (2026-03-21)
  - [x] **GREEN**: в `ParkFormUiState.canSave` для create-режима убрано лишнее условие `hasChanges`.

### Non-blocking, закрыто ✅

- [x] Исправлено `gradeString/sizeString`: переименованы в `gradeStringRes`/`sizeStringRes`, возвращают `Int` (StringRes id) вместо строки с числовым id. ✅ (2026-03-21)
- [x] Переименовано `BackWithCreatedPark` -> `BackWithSavedPark` в `ParkFormNavigationAction`. ✅ (2026-03-21)

---

## Основные компоненты

### iOS ParkForm (ParkGrade/ParkSize)

| Поле | Тип | Описание |
|------|-----|----------|
| `address` | String | Адрес площадки |
| `latitude` | String | Широта |
| `longitude` | String | Долгота |
| `cityId` | Int? | ID города |
| `typeId` | Int | Тип площадки (ParkGrade) |
| `sizeId` | Int | Размер площадки (ParkSize) |
| `photosCount` | Int | Количество существующих фото |
| `newMediaFiles` | [MediaFile] | Новые фото |

### Android ParkType (аналог ParkGrade)

| Значение | rawValue | description (R.string) |
|----------|----------|------------------------|
| SOVIET | 1 | `soviet_park` |
| MODERN | 2 | `modern_park` |
| COLLARS | 3 | `collars_park` |
| LEGENDARY | 6 | `legendary_park` |

### Android ParkSize

| Значение | rawValue | description (R.string) |
|----------|----------|------------------------|
| SMALL | 1 | `small_park` |
| MEDIUM | 2 | `medium_park` |
| LARGE | 3 | `large_park` |

---

## Этап 1 (TDD): Модель ParkForm ✅

### 1.1 Актуализировать существующую `ParkForm` (без дублирования модели)

- [x] **RED**: написать unit-тесты для `ParkForm` по матрице iOS `ParkFormTests` (create/update ready/not-ready, `equals`, `imagesLimit`)
- [x] **GREEN**: использовать существующую `app/src/main/java/com/swparks/ui/model/ParkForm.kt`, не создавать вторую `ParkForm` в другом пакете
- [x] **GREEN**: добавить поля и поведение iOS-паритета: `photosCount: Int = 0`, `newMediaFiles`, `imagesLimit`
- [x] **GREEN**: добавить фабрики/конструкторы: из `Park` (edit mode) и из create-аргументов (`address`, `latitude`, `longitude`, `cityId`)
- [x] **GREEN**: добавить вычисляемые свойства `gradeStringRes` и `sizeStringRes` (для UI-отображения выбранных type/size через `stringResource()`)
- [x] **GREEN**: реализовать `isReadyToCreate` **в точности как в iOS**:
  - [x] Все строки `address`, `latitude`, `longitude` непустые после trim
  - [x] `cityId != null`
  - [x] `cityId != 0`
  - [x] `newMediaFiles` не пуст
- [x] **GREEN**: реализовать `isReadyToUpdate(old: ParkForm)` **в точности как в iOS**:
  - [x] Строки `address`, `latitude`, `longitude` валидны (не пустые после trim)
  - [x] Текущая форма `!= old` (есть реальные изменения)
- [x] **GREEN**: сверить equality-логику с iOS (`photosCount` не участвует в `equals`, `newMediaFiles` участвует)
- [x] **REFACTOR**: почистить дубли в валидации/утилитах, повторно прогнать тесты модели

### 1.2 Добавить string resources

**Заголовки экрана (TopAppBar):**
- [x] Create mode: `"Новая площадка"` → добавить как `R.string.new_park_title` (iOS: `newParkTitle`)
- [x] Edit mode: `"Площадка"` → уже существует `R.string.park_title`

**Метки полей:**
- [x] Проверить `soviet_park`, `modern_park`, `collars_park`, `legendary_park` (type) — уже есть
- [x] Проверить `small_park`, `medium_park`, `large_park` (size) — уже есть
- [x] `park_type` ("Тип площадки") — уже есть
- [x] `park_size` ("Размер площадки") — уже есть
- [x] `address` ("Адрес") — уже есть

**Кнопка сохранения:**
- [x] Использовать существующий `R.string.save` (без дублирования `park_form_save`, если нет отдельной UX-потребности)

**Диалог подтверждения закрытия:**
- [x] Принять решение и зафиксировать в кодстайле экрана:
  - [x] либо переиспользовать `event_form_confirm_close_title/message`
  - [x] либо добавить `park_form_confirm_close_title/message` как отдельные ключи
- [x] Обязательно синхронизировать `values/` и `values-ru/`

---

## Этап 2 (TDD): Data Layer и контракт сохранения ✅

### 2.1 Подключить существующий контракт сохранения

- [x] **RED**: написать/обновить unit-тесты use-case/repository boundary для сценариев create/edit с фото и без фото
- [x] **GREEN**: использовать существующий `SWRepository.savePark(id, form, photos)` (метод уже есть)
- [x] **GREEN**: не вводить новый `ParksRepository`, если функциональность закрывается текущим `SWRepository`
- [x] **GREEN**: зафиксировать mapping изображений: UI `Uri` -> `ByteArray` -> `savePark(..., photos)`
- [x] **GREEN**: для edit mode передавать `id`, для create mode передавать `id = null`

### 2.2 Проверить API-паритет параметров

- [x] **RED**: добавить тесты на корректный набор параметров в запросе (включая `city_id`, `type_id`, `class_id`, `photos`)
- [x] **GREEN**: подтвердить соответствие полей формы multipart-параметрам: `address`, `latitude`, `longitude`, `city_id`, `type_id`, `class_id`, `photos`
- [x] **GREEN**: зафиксировать обработку ошибок сохранения (сценарии create/edit)
- [x] **REFACTOR**: вынести повторяемый код формирования multipart в helper, не меняя поведение

---

## Этап 3 (TDD): ViewModel ✅

### 3.1 Создать `ParkFormViewModel`

- [x] **RED**: сначала описать контракт тестами `IParkFormViewModel` (state/events/actions)
- [x] **GREEN**: создать `IParkFormViewModel` interface
- [x] **GREEN**: создать `ParkFormViewModel` implementation
- [x] **GREEN**: параметры конструктора:
  - [x] mode (`Create`/`Edit(parkId)`)
  - [x] create-аргументы: `initialAddress`, `initialLatitude`, `initialLongitude`, `initialCityId`
  - [x] зависимости: `SWRepository`, `UserNotifier`, `Logger`

### 3.2 State & Actions

- [x] `ParkFormUiState`: `form`, `isSaving`, `hasChanges`, `canSave`, `mode`
- [x] `ParkFormAction`: `AddressChange`, `TypeChange`, `SizeChange`, `PhotoSelected`, `PhotoRemove`, `SaveClick`, `AddPhotoClick`
- [x] `ParkFormEvent`: `Saved`, `NavigateBack`, `ShowPhotoPicker`, `Error`

### 3.3 Логика

- [x] **RED**: тесты на инициализацию формы (create vs edit mode)
- [x] **RED**: тесты на `canSave` строго через `ParkForm.isReadyToCreate/isReadyToUpdate(old)` без упрощений
- [x] **RED**: тесты на обработку фото (`onPhotoSelected`, `onPhotoRemove`, `onAddPhotoClick`) и лимиты
- [x] **RED**: тесты на `SaveClick`: success/error, `isSaving`, эмит событий
- [x] **GREEN**: реализовать инициализацию формы (create vs edit mode)
- [x] **GREEN**: реализовать вычисление `canSave` только через методы формы
- [x] **GREEN**: реализовать обработку фото и учет `photosCount`/`imagesLimit`
- [x] **GREEN**: реализовать сохранение через repository `savePark()`
- [x] **REFACTOR**: упростить reducer/action-handlers, сохранить тесты зелеными
- [x] **PATCH после ревью**: `canSave` в create-режиме приведён к строгому iOS-паритету (без дополнительного `hasChanges`) ✅

### 3.4 Unit-тесты ViewModel

- [x] Обязательный минимум: create mode, edit mode, address/type/size change
- [x] Обязательный минимум: photo selection/limit, hasChanges, canSave (полный iOS-паритет условий)
- [x] Обязательный минимум: save success, save error

---

## Этап 4 (TDD): UI Layer (Compose) ✅

### 4.1 Создать `ParkFormScreen.kt`

**Структура (аналог EventFormScreen):**

```
ParkFormScreen
├── TopBar (CenterAlignedTopAppBar)
├── Content (Column + ScrollState)
│   ├── AddressField (SWTextField)
│   ├── ParkTypeSelectorSection (RadioButtons)
│   ├── ParkSizeSelectorSection (RadioButtons)
│   └── PickedImagesGrid
├── BottomBar (SaveButton + KeyboardAwareBottomBar)
├── LoadingOverlayView
├── ImagePreviewDialog
└── ConfirmCloseDialog
```

- [x] **GREEN**: Создан `ParkFormScreen.kt` (407 строк)
- [x] **GREEN**: Создан `FakeParkFormViewModel.kt` для androidTest
- [x] **GREEN**: Создан `ParkFormScreenTest.kt` (17 тестов)

### 4.2 Компоненты

#### 4.2.1 `ParkTypeRadioButtons` ✅

- [x] **GREEN**: аналог `GenderRadioButtons` в RegisterUserScreen.kt:473
- [x] **GREEN**: использует `RadioButton` из `com.swparks.ui.ds`
- [x] **GREEN**: `Column` с `selectableGroup()`
- [x] **GREEN**: итерация по `ParkType.entries`
- [x] **GREEN**: `selectedTypeId == parkType.rawValue`

#### 4.2.2 `ParkSizeRadioButtons` ✅

- [x] **GREEN**: аналогичная структура для `ParkSize.entries`
- [x] **GREEN**: `selectedSizeId == parkSize.rawValue`

#### 4.2.3 `ParkTypeSelectorSection` & `ParkSizeSelectorSection` ✅

- [x] Оборачивание в `FormCardContainer`
- [x] Передача `typeId`/`sizeId`, `enabled`, `onTypeChange`/`onSizeChange`

### 4.3 Навигация (частично)

- [x] `ParkFormNavigationAction`: `Back`, `BackWithSavedPark(Park)`
- [ ] Интеграция с AppNavigation / RootScreen (TODO) — Stage 5

### 4.4 UI-тесты ✅

- [x] **RED/GREEN**: тест: отображение всех полей формы
- [x] **RED/GREEN**: тест: radio buttons для type и size
- [x] **RED/GREEN**: тест: навигация back с подтверждением
- [x] **RED/GREEN**: тест: `Save` disabled/enabled по тем же условиям, что и в iOS (create/edit)
- [x] **REFACTOR**: ktlintCheck ✅ PASSED, testDebugUnitTest ✅ BUILD SUCCESSFUL

---

## Этап 5 (TDD): Навигация ✅ (Infrastructure) / ⚠ (Location integration)

### 5.1 Подключить существующие routes ✅

- [x] **GREEN**: использовать уже существующие `Screen.CreatePark` и `Screen.EditPark`
- [x] **GREEN**: реализовать `composable` для `CreatePark` route в `RootScreen`
- [x] **GREEN**: реализовать `composable` для `EditPark` route в `RootScreen`
- [x] **GREEN**: добавить `EditParkNavArgs` и `consumeEditParkArgs()` в `ParkNavArgs.kt`
- [x] **GREEN**: добавить `EditParkNavArgsViewModel` в `NavArgsViewModels.kt`
- [x] **GREEN**: добавить `navigateToEditPark()` в `ParkNavigationCoordinator.kt`
- [x] **GREEN**: добавить `CreateParkNavArgs` и `consumeCreateParkArgs()` в `ParkNavArgs.kt`
- [x] **GREEN**: добавить `CreateParkNavArgsViewModel` в `NavArgsViewModels.kt`
- [x] **GREEN**: добавить `navigateToCreatePark()` в `ParkNavigationCoordinator.kt`
- [x] **GREEN**: создать модель `NewParkDraft` (аналог iOS `NewParkMapModel`)
- [x] **GREEN**: добавить unit-тесты для `NewParkDraft` (18 тестов)
- [x] **PATCH после ревью (P1)**: включить переход на `EditPark` из `ParkDetail` (через event + coordinator) ✅
- [x] **PATCH после ревью (P1)**: добавить обратный канал результата `EditPark` -> `ParkDetail` refresh ✅

### 5.2 Определить Android-аналог `NewParkMapModel` ✅

- [x] **GREEN**: ввести модель create-черновика `NewParkDraft` с полями `latitude`, `longitude`, `address`, `cityId`, `lastLocationRequestDate`
- [x] **GREEN**: добавить флаг/правило `shouldPerformGeocode` по iOS-логике: geocode нужен, если `address` пуст или `cityId` отсутствует/равен 0
- [x] **GREEN**: добавить флаг/правило `shouldRequestLocation`: true если нет координат или прошло >10 сек с последнего запроса
- [x] **GREEN**: описать источник координат (будет карта/геолокация пользователя через `LocationPermissionHandler`)
- [x] **GREEN**: добавить helper methods: `withCoordinates()`, `withGeocodingData()`, `withoutAddress()`
- [x] **REFACTOR**: убрать дубли create-аргументов в навигации/VM ✅

### 5.3 Геолокация: разрешения и получение координат ✅ (Infrastructure + FAB integration + Tests)

**Текущее состояние (2026-03-22):**
- ✅ Permissions `ACCESS_FINE_LOCATION`/`ACCESS_COARSE_LOCATION` добавлены в AndroidManifest.xml
- ✅ Модель `NewParkDraft` готова с логикой `shouldRequestLocation`
- ✅ `CreateParkFab` интегрирован с `LocationPermissionState` (internal class в ParksRootScreen.kt)
- ✅ `LocationPermissionAlertDialog` composable реализован
- ✅ При granted -> создаётся `NewParkDraft.EMPTY` и вызывается `navigateToCreatePark()`
- ✅ **Тесты добавлены (2026-03-22)**:
  - `ParksRootScreenTest`: исправлены баги (assertDoesNotExist вместо assertIsDisplayed), добавлен тест `whenUserIsAuthorized_fabIsEnabled`
  - `LocationPermissionAlertDialogTest`: 7 тестов для dialog-сценариев (denied forever, just denied, dismiss, confirm, open settings)
- ⚠️ `LocationPermissionHandler` интерфейс существует, но не используется (ParksRootScreen использует internal State)
- ⚠️ Не реализован actual location retrieval (получение реальных координат устройства)
- ⚠️ Не реализован geocoding для заполнения address/cityId

**UX-требование (FAB + Alert при отказе):** ✅ ИНТЕГРИРОВАНО
- `CreateParkFab` доступна только авторизованным пользователям (`appState.isAuthorized`) ✅
- При нажатии `CreateParkFab`:
  - **Если разрешение уже получено** → переход на `Screen.CreatePark` с `NewParkDraft.EMPTY` ✅ (координаты пока не получаются)
  - **Если разрешение не получено** → показать `AlertDialog` ✅
  - **Если разрешение отклонено навсегда** → показать `AlertDialog` с `"Открыть настройки"` ✅
- [x] **GREEN**: добавить `ACCESS_FINE_LOCATION` и `ACCESS_COARSE_LOCATION` в `AndroidManifest.xml` ✅
- [x] **GREEN**: создать `LocationPermissionAlertDialog` composable ✅
- [x] **GREEN**: добавить локализованные строки (`values/` + `values-ru/`) ✅
  - `location_permission_alert_title`, `location_permission_alert_message`, `location_permission_denied`, `location_permission_need_in_settings`, `open_settings`, `allow_location_access`
- [x] **GREEN**: добавить UI-тесты для `LocationPermissionAlertDialog` ✅ (2026-03-22)
  - Тесты: visible/denied forever, visible/not denied, not visible, confirm clicked, dismiss clicked, open settings clicked
- [x] **GREEN**: исправить баги в `ParksRootScreenTest` ✅ (2026-03-22)
  - `whenUserIsNotAuthorized_fabIsNotDisplayed` теперь использует `assertDoesNotExist()`
  - Добавлен `whenUserIsAuthorized_fabIsEnabled`
- [ ] **TODO (Stage 6.1)**: реализовать actual location retrieval + geocoding

**Спецификация строк для Android (обязательная, без доп. трактовки):**
- Новые ключи в `app/src/main/res/values/strings.xml`:
  - `location_permission_alert_title`: `"Location access"`
  - `location_permission_alert_message`: `"To create a park, allow geolocation access to determine the coordinates of a new park."`
  - `open_settings`: `"Open settings"`
  - `location_permission_denied`: `"You have not granted access to geolocation"`
  - `location_permission_need_in_settings`: `"To display your location, please give access to geolocation in settings"`
- Новые ключи в `app/src/main/res/values-ru/strings.xml`:
  - `location_permission_alert_title`: `"Доступ к геолокации"`
  - `location_permission_alert_message`: `"Для создания площадки необходимо разрешить приложению доступ к геолокации для определения координат новой площадки."`
  - `open_settings`: `"Открыть настройки"`
  - `location_permission_denied`: `"Запрещен доступ к геолокации"`
  - `location_permission_need_in_settings`: `"Для отображения твоего местоположения необходимо разрешить доступ к геолокации в настройках"`
- Кнопки в `AlertDialog`:
  - Confirm (первичный сценарий): переиспользовать существующий `allow`/`grant` ключ, если уже есть; иначе добавить `allow_location_access` (`"Allow"` / `"Разрешить"`)
  - Dismiss: переиспользовать существующий `cancel`

---

### 5.4 Location retrieval + Geocoding (TDD, iOS-parity) 🔄

**Обзор:** iOS-паритет для create-flow:
- Шаг 1 (`requestLocationForNewPark`): получить/обновить координаты и передать `NewParkDraft` в форму
- Шаг 2 (`performGeocodingForNewPark`): выполнить geocoding уже на экране формы, если `draft.shouldPerformGeocode == true`

**Источники данных (референс iOS):**
- iOS: `NewParkMapModel` (SwiftUI-WorkoutApp)
- iOS: `ParksMapScreen+ViewModel.requestLocationForNewPark()`
- iOS: `ParkFormScreen.performGeocodingForNewPark()`
- iOS: `GeocodingService.makeAddressAndCityId()`
- iOS: `SWAddress.makeCityId(with:)`
- Location: `FusedLocationProviderClient` (Google Play Services) или `LocationManager` (fallback)
- Geocoding: `android.location.Geocoder.getFromLocation(...)` (API-aware обертка)
- City lookup: `CountriesRepository.getAllCities()` → match by `locality` (case-insensitive, first match как в iOS `SWAddress.findCity`)

**Типы данных (важно):**
- `NewParkDraft.cityId: Int?` — iOS-совместимость (сервер ожидает Int)
- `City.id: String` — локальный JSON использует String
- `User.cityID: Int?` — серверный ID города как Int
- **Требуется:** конвертация String → Int при city lookup (через `.toIntOrNull()`)

**Порядок выполнения Stage 5.4 (строго по TDD, без перестановок):**
1. `5.4.1` — контракты и модели (интерфейсы/типы)
2. `5.4.2` — data-реализации location/geocoding (с unit-тестами)
3. `5.4.3` — city lookup use-case (с unit-тестами)
4. `5.4.5` — DI wiring в `AppContainer` (после готовых реализаций)
5. `5.4.4` — интеграция в `ParksRootScreen` только location + навигация (androidTest)
6. `5.4.4b` — geocoding в create-flow `ParkFormViewModel` (unit-тесты)
7. `5.4.4c` ✅ — fail fast в `DefaultCreateParkLocationHandler` при ошибке location (координаты критичны)
8. `5.4.6` + `5.4.7` + `5.4.8` — строки, интеграционные проверки, type-safety и финальный рефактор

**Следующий refactor-этап (после 5.4.4c):**
- [x] Вынести тестируемую create-flow логику из `LocationPermissionState` в отдельный handler/use-case (чистый unit-test boundary без Android runtime)
  - Создан `ICreateParkLocationHandler` интерфейс в `domain/usecase/`
  - Создан `DefaultCreateParkLocationHandler` с логикой `handlePermissionGranted()` (location retrieval + NewParkDraft creation)
  - `LocationPermissionState` теперь делегирует в `ICreateParkLocationHandler`, не содержит Android-зависимостей кроме Context для permission check
- [x] Подготовить seam для подмены `LocationService`/`UserNotifier` в UI integration tests (без доступа к реальному `Application.container`)
  - `ICreateParkLocationHandler` - единая точка подмены для UI integration tests
  - В `AppContainer` добавлен `createParkLocationHandler: ICreateParkLocationHandler`
- [x] Прогнать регрессию `make lint` + `make test` после рефакторинга
  - ktlintCheck: BUILD SUCCESSFUL
  - testDebugUnitTest: BUILD SUCCESSFUL (120 unit-test files)
  - assembleDebug: BUILD SUCCESSFUL
- [x] **`5.4.4c`** ✅ — `invoke()` возвращает `Result<NewParkDraft>`. При `onFailure` — `userNotifier.handleError(AppError.Generic(...))` + `Result.failure()`. `LocationPermissionState.handlePermissionGranted()` навигирует только при `result.isSuccess`.

---

#### 5.4.1 Domain Layer: Location Service Interface ✅

**Файлы:**
- `domain/provider/LocationService.kt` (новый интерфейс)
- `domain/provider/GeocodingService.kt` (новый интерфейс)
- `domain/model/GeocodingResult.kt` (новый data class)
- `domain/model/LocationCoordinates.kt` (новый data class)

**Контракт LocationService:**
```kotlin
interface LocationService {
    suspend fun getCurrentLocation(): Result<LocationCoordinates>
}
```

**Контракт GeocodingService:**
```kotlin
interface GeocodingService {
    suspend fun reverseGeocode(latitude: Double, longitude: Double): Result<GeocodingResult>
}
```

**Данные:**
```kotlin
data class LocationCoordinates(
    val latitude: Double,
    val longitude: Double
)

data class GeocodingResult(
    val address: String,      // полный адрес
    val locality: String?,    // город/населенный пункт
    val countryName: String?
)
```

- [x] **GREEN**: создать `LocationService` интерфейс
- [x] **GREEN**: создать `GeocodingService` интерфейс
- [x] **GREEN**: создать `LocationCoordinates` и `GeocodingResult` data classes
- [x] **REFACTOR**: проверить нейминг/пакеты, чтобы контракты не тянули Android-зависимости

---

#### 5.4.2 Data Layer: Location Service Implementation ✅

**Файлы:**
- `data/provider/LocationServiceImpl.kt` (реализация)
- `data/provider/GeocodingServiceImpl.kt` (реализация)

**Build/deps (обязательно):**
- добавить `com.google.android.gms:play-services-location` в `app/build.gradle.kts`

**LocationServiceImpl:**
- Использует `FusedLocationProviderClient` из Google Play Services
- Привязывает `Looper` к main thread для callback
- Таймаут: 10 секунд (matching `LOCATION_STALE_THRESHOLD_MILLIS`)
- Fallback: при отсутствии Google Play Services — `LocationManager`

**GeocodingServiceImpl:**
- Использует `android.location.Geocoder`
- Собирает `address` из полей placemark (country/admin/subAdmin/locality/subLocality/thoroughfare/subThoroughfare) с удалением дублей (по аналогии `SWAddress.makeAddress`)
- Извлекает `Locality` → `locality`
- Обрабатывает `IOException` / пустой результат (`noPlacemarkFound`) / невозможность собрать адрес (`failedToCreateAddress`)

- [x] **RED**: написать unit-тесты для `LocationServiceImpl` (mock `FusedLocationProviderClient`)
- [x] **RED**: написать unit-тесты для `GeocodingServiceImpl` (mock `Geocoder`)
- [x] **GREEN**: реализовать `LocationServiceImpl` с `FusedLocationProviderClient`
- [x] **GREEN**: реализовать `GeocodingServiceImpl` с `Geocoder`
- [x] **REFACTOR**: убрать дубли callback/timeout-логики, повторно прогнать unit-тесты

---

#### 5.4.3 Domain Layer: City Lookup by Coordinates ⚠

**Файл:** `domain/usecase/FindCityByCoordinatesUseCase.kt` (новый)

**Контракт:**
```kotlin
interface IFindCityByCoordinatesUseCase {
    suspend operator fun invoke(locality: String?, latitude: Double, longitude: Double): Int?
}
```

**Логика:**
1. Если `locality` != null → search `CountriesRepository.getAllCities()` by name (case-insensitive)
2. Если найден один city → вернуть его `id.toIntOrNull()`
3. Если city не найден или `id` не конвертируется в Int → вернуть `null`

**Fallback:** вызывающий код (`ParkForm` create-flow) использует `currentUser.cityID` при `null`

- [ ] **RED**: написать unit-тесты для `FindCityByCoordinatesUseCase`:
  - `invoke_whenLocalityMatches_returnsCityId`
  - `invoke_whenLocalityNull_returnsNull`
  - `invoke_whenCityIdIsNotInt_returnsNull`
  - `invoke_whenNoMatch_returnsNull`
- [ ] **GREEN**: реализовать `FindCityByCoordinatesUseCase`
- [ ] **GREEN**: подключить `CountriesRepository` через конструктор
- [ ] **REFACTOR**: вынести нормализацию locality (trim/lowercase) в helper без изменения поведения

---

#### 5.4.4 ParksRootScreen: Integration ⚠

**Файл:** `ui/screens/parks/ParksRootScreen.kt`

**Изменения:**

1. **Добавить dependencies в `LocationPermissionState`:**
   - `LocationService`
   - `UserNotifier` (для warning при ошибке location)

2. **Изменить flow после `onPermissionGranted()`:**
   ```
   permission granted
       → getCurrentLocation() via LocationService
       → if success: build NewParkDraft with coordinates (address empty)
       → if valid geocoding cache exists for coordinate: reuse cache.address/cache.cityId
       → navigateToCreatePark(draft)
       → if location fails:
           → build NewParkDraft.EMPTY (как сейчас)
           → navigateToCreatePark(draft)
           → show warning notification (iOS-parity)
   ```

3. **Fallback в этом слое:**
   - Если location не удалось получить — использовать `NewParkDraft.EMPTY`
   - Показать warning notification о невозможности определить местоположение

4. **Важно (iOS parity):**
   - Основной geocoding выполняется **после навигации** в `ParkForm` (не в `ParksRootScreen`)
   - `ParksRootScreen` отвечает за координаты + permission-flow + optional cache

- [ ] **RED**: написать **androidTest** для `ParksRootScreen` (через public API):
  - `onFabClicked_whenPermissionGranted_fetchesLocationAndNavigates`
  - `onFabClicked_whenLocationFails_navigatesWithEmptyDraft`
- [ ] **GREEN**: изменить `LocationPermissionState` — добавить `LocationService` dependency
- [ ] **GREEN**: реализовать fetch location → build draft → navigate flow (без обязательного geocoding на этом шаге)
- [ ] **GREEN**: fallback с `currentUser.cityID` реализовать в `ParkForm` (см. 5.4.4b)
- [ ] **GREEN**: показать warning notification при неудаче location fetch
- [ ] **REFACTOR**: при необходимости вынести тестируемую часть из `LocationPermissionState` в отдельный handler/use-case

---

#### 5.4.4b ParkForm: Geocoding on Create ⚠

**Файлы:**
- `ui/viewmodel/ParkFormViewModel.kt`
- `ui/model/ParkFormMode.kt` (при необходимости)

**iOS-референс:** `ParkFormScreen.performGeocodingForNewPark()`

**Логика:**
1. Только для `ParkFormMode.Create`
2. Если `draft.shouldPerformGeocode == false` → ничего не делать
3. Иначе выполнить `GeocodingService.reverseGeocode(latitude, longitude)`
4. `cityId` определить через `FindCityByCoordinatesUseCase(locality, latitude, longitude)`
5. Успех: обновить `form.address` и `form.cityId`
6. Ошибка geocoding/city lookup:
   - `form.cityId = currentUser.cityID` (если есть)
   - если fallback недоступен, оставить `cityId = null` и позволить ручной ввод
   - показать user notification/error (iOS parity)

- [ ] **RED**: unit-тесты `ParkFormViewModel` для geocoding веток create-mode
- [ ] **GREEN**: реализовать geocoding при инициализации create-формы
- [ ] **GREEN**: fallback на `currentUser.cityID` при geocoding/city lookup ошибках
- [ ] **REFACTOR**: сохранить единый поток событий ошибок/уведомлений через `UserNotifier`

---

#### 5.4.4c DefaultCreateParkLocationHandler: Fail on Location Error ✅

**Файл:** `domain/usecase/DefaultCreateParkLocationHandler.kt`

**Проблема:** текущая реализация `onFailure` возвращает `NewParkDraft.EMPTY` и позволяет навигировать на экран создания без координат. Координаты критичны для создания park — ручной ввод координат отсутствует. Навигация без координат бессмысленна.

**Новый контракт `ICreateParkLocationHandler`:**
```kotlin
interface ICreateParkLocationHandler {
    suspend operator fun invoke(): Result<NewParkDraft>
}
```

**Логика:**
1. `locationService.getCurrentLocation().fold()`
2. `onSuccess`: вернуть `Result.success(NewParkDraft.EMPTY.withCoordinates(...))`
3. `onFailure`: показать error через `userNotifier` → вернуть `Result.failure(...)`

**Контракт вызова в `LocationPermissionState`:**
```kotlin
private fun handlePermissionGranted() {
    coroutineScope.launch {
        val result = createParkLocationHandler()
        result.onSuccess { draft ->
            navigateToCreatePark(draft)
        }
        // onFailure: не навигируем, error уже показан в handler
    }
}
```

**Информирование пользователя:**
- Ошибка location → `userNotifier.handleError(AppError.Generic("Не удалось определить местоположение. Проверьте разрешения и попробуйте снова."))`

- [x] **RED**: написать unit-тест `invoke_whenLocationFails_returnsFailureResult`
- [x] **RED**: написать unit-тест `invoke_whenLocationFails_doesNotShowInfoNotification` (а `handleError`)
- [x] **GREEN**: изменить `ICreateParkLocationHandler.invoke()` → `Result<NewParkDraft>`
- [x] **GREEN**: в `onFailure` вызывать `userNotifier.handleError(AppError.Generic(...))` вместо `showInfo`
- [x] **GREEN**: в `onFailure` возвращать `Result.failure()` вместо `NewParkDraft.EMPTY`
- [x] **GREEN**: обновить `LocationPermissionState.handlePermissionGranted()` — использовать `result.onSuccess`, не навигировать при ошибке
- [x] **REFACTOR**: обновить unit-тесты для нового контракта (`Result<NewParkDraft>`)

**Примечание:** в интерфейсе `UserNotifier` нет метода `showError(string)`, используется `handleError(AppError)` с `AppError.Generic`.

---

#### 5.4.5 DI: Add Location Services to AppContainer ⚠

**Файл:** `data/AppContainer.kt`

**Изменения:**
```kotlin
// Location Services
val locationService: LocationService
val geocodingService: GeocodingService
val findCityByCoordinatesUseCase: IFindCityByCoordinatesUseCase
```

- [ ] **GREEN**: добавить `LocationServiceImpl` в `DefaultAppContainer`
- [ ] **GREEN**: добавить `GeocodingServiceImpl` в `DefaultAppContainer`
- [ ] **GREEN**: добавить `FindCityByCoordinatesUseCase` в `DefaultAppContainer`
- [ ] **REFACTOR**: добавить/обновить тест `DefaultAppContainerTest` для новых dependency

---

#### 5.4.6 String Resources ⚠

**Новые ключи (values/strings.xml):**
- `location_fetch_failed`: `"Unable to determine location"`
- `geocoding_failed`: `"Unable to determine address"`
- `city_not_found_use_manual`: `"City is not determined. Please specify it manually."`

**Новые ключи (values-ru/strings.xml):**
- `location_fetch_failed`: `"Не удалось определить местоположение"`
- `geocoding_failed`: `"Не удалось определить адрес"`
- `city_not_found_use_manual`: `"Город не определен. Укажите вручную."`

- [ ] **GREEN**: добавить строки в `values/strings.xml`
- [ ] **GREEN**: добавить строки в `values-ru/strings.xml`

---

#### 5.4.7 Integration Tests (ParksRootScreen) ⚠

**Файл:** `app/src/androidTest/java/com/swparks/ui/screens/parks/`

**Стратегия (зафиксировано):** тестировать через public API `ParksRootScreen` + `RootScreen`; не писать unit-тесты на private `LocationPermissionState`.

- [ ] **RED**: написать integration tests для full create-flow
- [ ] **GREEN**: покрыть тестами permission/location ветки в `ParksRootScreen`
- [ ] **GREEN**: geocoding fallback покрыть unit-тестами `ParkFormViewModel`
- [ ] **REFACTOR**: удалить/обновить устаревшие тест-кейсы, которые дублируют новую матрицу

---

#### 5.4.8 Type Safety Fix ⚠

**Проблема:** `NewParkDraft.cityId: Int?` но `City.id: String`

**Решение:** Конвертация в `FindCityByCoordinatesUseCase`:
```kotlin
val cityId: Int? = city.id.toIntOrNull()
```

- [ ] **GREEN**: убедиться что `FindCityByCoordinatesUseCase` выполняет `toIntOrNull()` conversion
- [ ] **REFACTOR**: проверить что iOS API действительно ожидает Int для city_id

---

## Этап 6 (TDD): Интеграция с существующими экранами ⚠ (Edit complete, Create pending)

### 6.1 ParksRootScreen (Create flow entrypoint) ⚠ (FAB integrated, location/geocoding → Stage 5.4)

**Текущее состояние (2026-03-22):**
- ✅ `CreateParkFab` существует и показывается для авторизованных пользователей
- ✅ FAB интегрирован с `LocationPermissionState` ( запрос permission, показ диалогов)
- ✅ При granted вызывается `navigateToCreatePark(source, NewParkDraft.EMPTY)` — **координаты пока не получаются**
- 🔄 **Stage 5.4**: Location retrieval + Geocoding (план добавлен)

- [ ] **RED**: интеграционные UI/Nav тесты create-flow в границах `ParksRootScreen`/`RootScreen`
- [ ] **GREEN**: после Stage 5.4 — обновлять `NewParkDraft` с полученными координатами и timestamp
- [ ] **GREEN**: после Stage 5.4 — geocoding запускать на этапе `ParkForm` (не в `ParksRootScreen`) с fallback:
  - primary: `reverseGeocode` -> `address` + `locality`
  - `cityId` mapping: поиск по `CountriesRepository` (name/locality match, iOS-подход first-match)
  - fallback при ошибках: `currentUser.cityID` (если задан), иначе ручной ввод

### 6.2 ParkDetailScreen (Edit flow) ✅

**Текущее состояние:**
- ✅ `NavigateToEditPark` event добавлен в `ParkDetailEvent`
- ✅ `ParkDetailViewModel.onEditClick()` эмитит событие навигации
- ✅ `ParkDetailScreen` обрабатывает событие и вызывает `onNavigateToEditPark(park)`
- ✅ `RootScreen` передаёт callback и вызывает `navigateToEditPark()` через coordinator
- ✅ После сохранения `updatedPark` передаётся через `savedStateHandle` и обновляет `ParkDetail`
- ✅ Unit-тест `onEditClick_thenEmitsNavigateToEditPark` существует
- ✅ Unit-тест `onParkUpdated_whenCalled_thenReloadsPark` существует

- [ ] **RED**: интеграционные тесты edit-flow из `ParkDetail` и refresh после возврата (UI/Nav integration tests)

### 6.3 Интеграционные тесты create-flow без архитектурного конфликта ⚠

**Проблема архитектуры:**
- `RootScreen` использует runtime `AppContainer` из `Application`; прямой e2e-тест "FAB -> реальное сохранение" нестабилен и зависит от сети.

**Тестовая стратегия (обязательная):**
- [ ] **RED**: `ParksRootScreen` Compose UI-тесты для веток permission (`granted`, `denied`, `forever denied`)
- [ ] **RED**: `ParkNavigationCoordinator` unit-тесты на передачу `NewParkDraft` в `savedStateHandle`
- [ ] **RED**: `RootScreen` UI/Nav integration test: FAB -> route `CreatePark` -> `ParkFormMode.Create` получает prefill из `CreateParkNavArgs`
- [ ] **GREEN**: покрытие "успешного сохранения" оставлять на уровне unit-тестов `ParkFormViewModel`/repository boundary (без добавления сетевой зависимости в UI tests)

**Текущее состояние тестов:**
- ✅ `ParksRootScreenTest.kt` существует (3 теста FAB + авторизация)
- ✅ `LocationPermissionAlertDialogTest` существует (в том же файле; 6 сценариев)
- ⚠️ Нужны тесты на содержимое draft после location-flow и на интеграцию prefill в create-form

---

## Критерии завершения

### Выполнено ✅

- [x] Этапы 1-4 пройдены по TDD-циклу RED -> GREEN -> REFACTOR
- [x] `ParkForm` актуализирована без дублирования модели и с iOS-parity валидацией (35 unit-тестов)
- [x] `ParkFormViewModel` реализован с unit-тестами (33 unit-теста)
- [x] `ParkFormScreen` отображает address field
- [x] Radio buttons для type (ParkType) работают
- [x] Radio buttons для size (ParkSize) работают
- [x] PickedImagesGrid интегрирован
- [x] Save button сохраняет данные (логика в ViewModel)
- [x] Навигация back с подтверждением при изменениях (`ConfirmCloseDialog`)
- [x] UI-тесты для ParkFormScreen (17 тестов в `ParkFormScreenTest.kt`)
- [x] **Этап 5 завершён (Infrastructure)**:
  - [x] `CreatePark` route → `ParkFormScreen` (create mode) с поддержкой `NewParkDraft`
  - [x] `EditPark` route → `ParkFormScreen` (edit mode)
  - [x] `EditParkNavArgs` / `EditParkNavArgsViewModel` / `navigateToEditPark()`
  - [x] `CreateParkNavArgs` / `CreateParkNavArgsViewModel` / `navigateToCreatePark()`
  - [x] `NewParkDraft` model с `shouldRequestLocation` и `shouldPerformGeocode` (18 unit-тестов)
  - [x] `ACCESS_FINE_LOCATION` / `ACCESS_COARSE_LOCATION` permissions в AndroidManifest.xml
  - [x] `ParkDetail -> EditPark` переход подключен (event + coordinator)
  - [x] update-result из `ParkFormScreen` обратно в `ParkDetail` подключен (savedStateHandle)
- [x] **Этап 6.2 завершён (Edit flow)**:
  - [x] `onEditClick` → emit `NavigateToEditPark` event
  - [x] Handle event in `ParkDetailScreen` → call `navigateToEditPark()`
  - [x] После сохранения: `onParkUpdated(parkId)` для refresh
  - [x] Unit-тесты для edit-flow существуют
- [x] `make lint` (ktlintCheck) проходит
- [x] `make test` (testDebugUnitTest) проходит (120 unit-test files)
- [x] detekt (37 weighted issues, на пороге)

### В процессе (Stage 5.4 / 6.3) 🔄

- [ ] **Stage 5.4**: Location retrieval + Geocoding (TDD-план в работе)
  - [x] LocationService interface + implementation ✅
  - [x] GeocodingService interface + implementation ✅
  - [x] FindCityByCoordinatesUseCase ✅ (Stage 5.4.3)
  - [x] ParksRootScreen integration ✅ (Stage 5.4.4)
  - [x] ParkForm create geocoding integration ✅ (Stage 5.4.4b)
  - [x] DI wiring ✅ (Stage 5.4.5)
  - [x] **Refactor (после 5.4.4b)**: Вынесение create-flow логики в `ICreateParkLocationHandler` ✅
  - [x] **Refactor 5.4.4c**: fail fast при ошибке location (`Result<NewParkDraft>`, `handleError`) ✅
- [ ] Интеграционные тесты для create-flow (UI/Nav) по стратегии Stage 6.3 (без сетевой зависимости)
- [ ] Интеграционные тесты для edit-flow (UI/Nav integration tests)

---

## Заметки по реализации

1. **RadioButtons**: Использовать паттерн из `GenderRadioButtons` (RegisterUserScreen.kt:473)
2. **FormCardContainer**: Используется для группировки селекторов
3. **PickedImagesGrid**: Готовый компонент, аналогичный EventFormScreen
4. **ImagePreviewDialog**: Готовый компонент
5. **LoadingOverlay**: Готовый компонент `LoadingOverlayView`
6. **KeyboardAwareBottomBar**: Аналогично EventFormScreen для кнопки Save

---

## Out of Scope (следующие фазы)

- Offline поддержка
- Дополнительная валидация формата координат (сверх iOS-паритета)
