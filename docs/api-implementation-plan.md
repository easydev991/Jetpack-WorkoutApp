# План реализации API в Jetpack-WorkoutApp

## Обзор

План реализации всех запросов к серверу workout.su в Android-приложении, аналогично iOS-версии.

## Структура API протоколов в iOS

В iOS-проекте используются следующие протоколы клиентов: AuthClient, CommentsClient, CountriesClient, EventsClient, FriendsClient, JournalsClient, MessagesClient, ParksClient, ParksUpdaterClient, PhotosClient, ProfileClient.

---

## ✅ Завершенные этапы

### Этапы 1-7.1 ✅

Реализовано 57 эндпоинтов API, улучшена обработка ошибок и десериализация (27 unit-тестов).

---

## Баги

### Баг #1: Краш EventsScreen из-за неподдерживаемого формата даты ✅

Добавлена поддержка форматов даты с часовым поясом `+HH:MM` и `-HH:MM` в `FlexibleDateDeserializer`.

### Баг #2: Метод getSocialUpdates использует несуществующий endpoint ✅

Реализован через 4 параллельных запроса с агрегацией на клиенте, аналогично iOS.

### Баг #3: Записи в дневниках неправильно реализованы как комментарии ✅

Переписана логика через правильные endpoints (`/messages`), аналогично iOS.

### Баг #4: Дублирование обработки ошибки 401 в SWRepository ✅

Удалены 34 дублирующие проверки из SWRepository, оставлена централизованная обработка в `AuthInterceptor`.

### Баг #5: Метод clearToken() не сбрасывает токен авторизации ✅

Безопасное хранение токена через `SecureTokenRepository` с шифрованием Tink (AES-128-GCM-HKDF), `TokenInterceptor` добавляет токен в заголовок, `AuthInterceptor` очищает флаг при 401, `LoginUseCase`/`LogoutUseCase` сохраняют/очищают токен.

📄 Детальный план: [docs/auth-token-plan.md](./auth-token-plan.md)

### Баг #6: resetPassword - неправильное имя параметра в запросе ✅

Создана модель `ResetPasswordRequest` с полем `@SerialName("username_or_email")`.

### Баг #7: editJournalSettings - лишние параметры в теле запроса ✅

Удалены поля `journalId` и `userId` из модели `EditJournalSettingsRequest`.

---

## Приоритет реализации

### Низкий приоритет

- **Этап 8** - Рефакторинг kotlinx.serialization

---

## Примечания

1. **Безопасное разворачивание опционалов**: Всегда использовать `?.let`, `?:`, `checkNotNull` вместо `!!`
2. **Обработка ошибок**: Использовать `Result<T>` для API ответов
3. **Логирование**: Логи на русском языке
4. **Локализация**: Поддержка русского (ru) и английского (en) языков
5. **Тестирование**: Все сетевые функции тестировать только на моках, без реальных запросов
6. **Стиль кода**: Следовать правилам из `.cursor/rules/code-style.mdc`
7. **Архитектура**: MVVM + Clean Architecture, ручной DI (без Hilt)

---

## Этап 8: Полный рефакторинг kotlinx.serialization (БУДУЩЕЕ)

**Задача:** Реализовать полный рефакторинг для использования встроенных возможностей kotlinx.serialization и kotlinx-datetime.

**План:**

1. Добавить зависимость `kotlinx-datetime`
2. Создать кастомный сериализатор дат
3. Обновить `AppContainer` с `JsonNamingStrategy.SnakeCase`
4. Рефакторинг 16 файлов моделей: удалить `@SerialName`, заменить `String` на `Instant`/`LocalDate`
5. Обновить unit тесты

**Рекомендация:** Выполнять только после стабильной работы текущей реализации и тщательного тестирования.

---

## Сводная таблица реализации

| Этап | Название | Статус |
|------|----------|---------|
| Этапы 1-7 | Полная реализация API | ✅ Завершен (57 из 57 эндпоинтов) |
| Этап 7.1 | Улучшения обработки ошибок и десериализации | ✅ Завершен |
| Этап 8 | Рефакторинг kotlinx.serialization | 📅 Запланировано |

### Баги

| Баг | Описание | Статус |
|------|----------|---------|
| Баг #1 | Краш EventsScreen из-за неподдерживаемого формата даты | ✅ Исправлен |
| Баг #2 | getSocialUpdates использует несуществующий endpoint | ✅ Исправлен |
| Баг #3 | Записи в дневниках неправильно реализованы как комментарии | ✅ Исправлен |
| Баг #4 | Дублирование обработки ошибки 401 в SWRepository | ✅ Исправлен |
| Баг #5 | Метод clearToken() не сбрасывает токен авторизации | ✅ Исправлен |
| Баг #6 | resetPassword - неправильное имя параметра в запросе | ✅ Исправлен |
| Баг #7 | editJournalSettings - лишние параметры в теле запроса | ✅ Исправлен |

---

## Расхождения с iOS-версией

### Расхождения в параметрах запросов

**✅ 0 расхождений** - Android-реализация соответствует iOS-версии.

**Примечание:** Все запросы реализованы ПРАВИЛЬНО - идентификаторы сущностей (userId, parkId, eventId, journalId, entryId) правильно передаются только через `@Path` в URL, а не дублируются в теле запроса.

### Ошибки реализации Android

**7 запланировано, 7 исправлено, 0 осталось** ✅

### Статистика реализации

| Категория | Всего в iOS | Реализовано | Уникальные для Android |
|-----------|-------------|-------------|------------------------|
| Авторизация и профиль | 7 | 7 | 0 |
| Друзья и черный список | 10 | 10 | 0 |
| Страны и города | 1 | 1 | 0 |
| Площадки | 15 | 15 | 0 |
| Мероприятия | 12 | 12 | 0 |
| Сообщения | 5 | 5 | 0 |
| Дневники | 7 | 7 | 0 |
| **ИТОГО** | **57** | **57** | **0** |

| **Общий прогресс:** 100% корректно реализовано (57 из 57 эндпоинтов iOS, 0 ошибок) |

---

## Рекомендации по приоритетам реализации

### Низкий приоритет

- **Этап 8** - Рефакторинг kotlinx.serialization (улучшение архитектуры и типизации дат)
