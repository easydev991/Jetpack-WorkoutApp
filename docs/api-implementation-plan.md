# План реализации API в Jetpack-WorkoutApp

## Обзор

План реализации всех запросов к серверу workout.su в Android-приложении, аналогично iOS-версии.

## Структура API протоколов в iOS

В iOS-проекте используются следующие протоколы клиентов: AuthClient, CommentsClient, CountriesClient, EventsClient, FriendsClient, JournalsClient, MessagesClient, ParksClient, ParksUpdaterClient, PhotosClient, ProfileClient.

---

## ✅ Завершенные этапы

### Этапы 1-7.1 ✅

Реализовано 65 из 65 эндпоинтов API, улучшена обработка ошибок и десериализация: модели данных, SWApi, SWRepository, тестирование (383 unit-теста), интеграция с DI, AuthInterceptor, ErrorResponse, гибкая десериализация числовых полей (IntStringSerializer, LongStringSerializer), поддержка форматов даты с часовым поясом.

---

## Баги

### Баг #1: Краш EventsScreen из-за неподдерживаемого формата даты ✅

**Проблема:** `FlexibleDateDeserializer` не поддерживал формат даты с часовым поясом `+00:00` (например: `2026-01-18T07:00:00+00:00`).

**Решение:** Добавлена поддержка форматов даты с часовым поясом `+HH:MM` и `-HH:MM` в `FlexibleDateDeserializer`.

**Статус:** ✅ Исправлен

**Выполнено:** Обновлен десериализатор для поддержки `+00:00` и `-00:00`, добавлены тесты, все 383 unit-теста проходят.

---

## Приоритет реализации

### Высокий приоритет

Реализовано:

1. **Улучшения обработки ошибок и десериализации (Этап 7.1)** - полностью реализовано:
   - Обработка 401 для автоматического логаута (AuthInterceptor)
   - Обработка текстов ошибок от сервера (ErrorResponse)
   - Созданы собственные исключения (ServerException, NetworkException)
   - Гибкая десериализация числовых полей (int-string для специфических полей в Park и Event)
   - Поддержка форматов даты с часовым поясом (+00:00, -00:00)

### Невыполненные этапы

- **Этап 8** - Рефакторинг kotlinx.serialization

---

## Примечания

1. **Безопасное разворачивание опционалов**: Всегда использовать `?.let`, `?:`, `checkNotNull` вместо `!!`
2. **Обработка ошибок**: Использовать `Result<T>` для API ответов
3. **Логирование**: Логи на русском языке
4. **Локализация**: Поддержка русского (ru) и английского (en) языков
5. **Тестирование**: Все сетевые функции тестировать только на моках, без реальных запросов
6. **Стиль кода**: Следовать правилам из `.cursor/rules/code-style.mdc`
7. **Архитектура**: MVVM + Clean Architecture, ручной DI (без Hilt)

---

## Этап 8: Полный рефакторинг kotlinx.serialization (БУДУЩЕЕ)

**Задача:** Реализовать полный рефакторинг для использования встроенных возможностей kotlinx.serialization и kotlinx-datetime.

**План:**

1. Добавить зависимость `kotlinx-datetime`
2. Создать кастомный сериализатор дат
3. Обновить `AppContainer` с `JsonNamingStrategy.SnakeCase`
4. Рефакторинг 16 файлов моделей: удалить `@SerialName`, заменить `String` на `Instant`/`LocalDate`
5. Обновить unit тесты

**Рекомендация:** Выполнять только после стабильной работы текущей реализации и тщательного тестирования.

---

## Сводная таблица реализации

| Этап | Название | Статус |
|------|----------|---------|
| Этапы 1-7 | Полная реализация API | ✅ Завершен (65 из 65 эндпоинтов) |
| Этап 7.1 | Улучшения обработки ошибок и десериализации | ✅ Завершен |
| Этап 8 | Рефакторинг kotlinx.serialization | 📅 Запланировано |

### Баги

| Баг | Описание | Статус |
|------|----------|---------|
| Баг #1 | Краш EventsScreen из-за неподдерживаемого формата даты | ✅ Исправлен |

---

## Расхождения с iOS-версией

### Расхождения в параметрах запросов

**✅ Нет расхождений** - Android-реализация полностью соответствует iOS-версии. Все 65 эндпоинтов реализованы.

### Статистика реализации

| Категория | Всего в iOS | Реализовано | Отсутствует |
|-----------|-------------|-------------|-------------|
| Авторизация и профиль | 7 | 7 | 0 |
| Остальные категории | 58 | 58 | 0 |
| **ИТОГО** | **65** | **65** | **0** |

**Общий прогресс:** 100% реализовано (65 из 65 эндпоинтов)

---

## Рекомендации по приоритетам реализации

### Высокий приоритет

1. **Улучшения обработки ошибок и десериализации (Этап 7.1)** - критические улучшения для API:
   - Обработка 401 для автоматического логаута (AuthInterceptor)
   - Обработка текстов ошибок от сервера (ErrorResponse)
   - Гибкая десериализация числовых полей (int-string для специфических полей в Park и Event)

**Примечание:** Обновление ViewModels и UI для показа ошибок и перенаправления на экран входа будет реализовано отдельно.

### Низкий приоритет

1. **Рефакторинг kotlinx.serialization** - улучшение архитектуры и типизации дат

---

## Детальный анализ

📄 `docs/api-implementation-comparison.md` - полное сравнение iOS и Android реализаций
