# План: Реактивное отображение профиля через currentUser

## Обзор

**Цель:** Переписать `ProfileRootScreen` для использования `currentUser: StateFlow<User?>` из `ProfileViewModel` вместо передачи параметра `user: User?` извне. Это обеспечит автоматическую подписку на изменения профиля при обновлении данных через репозиторий.

**Текущая проблема:**
- UI не реагирует на изменения профиля автоматически
- Данные пользователя передаются явно, вместо использования реактивного Flow
- Нарушение принципа единого источника правды

---

## Этап 1-4: Реализация реактивного профиля ✅

- [x] `ProfileViewModel`: `currentUser: StateFlow<User?>`, автоматическая загрузка при изменении
- [x] `ProfileRootScreen`: подписка через `collectAsState()`, компоненты используют реактивный источник
- [x] Интеграция: все вызовы обновлены, навигация работает без передачи `user`
- [x] Документация: обновлен `user_data_storage_plan.md`

---

## Этап 5: Тестирование

### Выполнено

- [x] Автоматические тесты, форматирование и сборка успешны
- [x] Preview функции реализованы

### Ручное тестирование (требуется устройство/эмулятор)

- [ ] Проверить отображение профиля при авторизации
- [ ] Проверить отображение экрана "Войти" при отсутствии пользователя
- [ ] Проверить автоматическое обновление UI при изменении профиля
- [ ] Проверить работу кнопки выхода

---

## Принципы разработки

**Архитектура:** MVVM, StateFlow для реактивного UI, единый источник правды (`currentUser` из ViewModel), Unidirectional Data Flow (репозиторий → ViewModel → UI).

**Правила:** MVVM, безопасное разворачивание опционалов (без `!!`), логи на русском.

**Важные детали:** Не удалять `currentUser` из ViewModel и `loadProfile()` метод; не удалять вычисление возраста в UI; корректно обрабатывать отсутствие пользователя.

**Будущие улучшения:** pull-to-refresh, автообновление при открытии экрана.

---

## Этап 6: Исправление проблемы с обновлением UI после авторизации ✅

**Проблема:** UI не обновляется автоматически после авторизации (локальное состояние в RootScreen, отсутствие реактивной связи).

**Выполнено:**
- [x] Data Layer: `UserPreferencesRepository.currentUserId` → `Flow<Long?>`, `SWRepository.getCurrentUserFlow()` через `flatMapLatest`
- [x] Domain Layer: `LoginUseCase`/`LogoutUseCase` управляют `userId`
- [x] UI Layer: `RootScreen` использует единый контейнер, `ProfileViewModel.currentUser` через `stateIn()`

---

## Этап 7: Тестирование сценария авторизации

### 7.0. Автоматическое тестирование (завершено)

- [x] 668 tests (604 unit + 64 instrumented), 100% successful
- [x] Исправлены 3 теста RootScreenTest (использованы локализованные сообщения вместо жестко заданных русских строк)

### 7.1. Тестирование авторизации с обновлением UI

- [ ] Открыть приложение, убедиться что показывается экран "Войти"
- [ ] Выполнить авторизацию
- [ ] Проверить логи: "Пользователь сохранен в репозиторий: userId"
- [ ] Перейти на вкладку "Профиль"
- [ ] Проверить, что UI показывает профиль автоматически БЕЗ перезапуска приложения
- [ ] Проверить, что профиль содержит корректные данные (имя, фото, страна, город)

### 7.2. Тестирование выхода из учетной записи

- [ ] Нажать кнопку "Выйти"
- [ ] Проверить логи: очистка данных пользователя
- [ ] Проверить, что UI обновляется на экран "Войти"
- [ ] Повторно авторизоваться
- [ ] Проверить, что UI снова показывает профиль

### 7.3. Тестирование обновления профиля

- [ ] Открыть вкладку "Профиль"
- [ ] Проверить, что профиль загружается с сервера (логи: "Загрузка профиля с сервера")
- [ ] Обновить профиль через сервер (если есть возможность)
- [ ] Проверить, что UI обновляется автоматически

### Критерии завершения

- [x] Все автоматические тесты успешны (668 tests)
- [ ] UI обновляется автоматически после авторизации и выхода
- [ ] Проблема с перезапуском приложения устранена

---

## Этап 8: Рефакторинг свойства isBusy в LoginUiState ✅

**Цель:** Вынести `isBusy` из View компонентов в `LoginUiState` для соблюдения принципа DRY.

**Выполнено:**
- [x] `LoginUiState`: добавлено вычисляемое свойство `isBusy` (возвращает `true` для `Loading`/`LoginSuccess`)
- [x] View компоненты: дублирующий код удален, все используют `uiState.isBusy`
- [x] Тесты: создан `LoginUiStateTest.kt` (9 тестов)

---

## Итоговая статистика тестов

**668 tests** (604 unit + 64 instrumented), 100% successful, 55 тестовых файлов.

**Изменения:**
- Добавлен `LoginUiStateTest.kt` (9 тестов)
- Исправлены 3 теста RootScreenTest (локализованные сообщения)
- Добавлен пакет `com.swparks.ui.state`
