# План доработки экрана профиля: добавление кнопок навигации

## Обзор

Добавить на экран профиля (`ProfileRootScreen`) кнопки навигации аналогично iOS-приложению и реализовать навигацию к экранам. Все данные для кнопок уже доступны в модели `User`.

## Назначение

Сделать Android-версию экрана профиля консистентной с iOS-версией, добавив кнопки для навигации к функционалу приложения.

## Что сделано

### UI экрана профиля

- ✅ `ProfileRootScreen` - полная реализация экрана с кнопками навигации
- ✅ Вертикальная прокрутка (verticalScroll) и Column с равными отступами
- ✅ Все кнопки из iOS-приложения реализованы
- ✅ `ProfileTopAppBar` с кнопкой поиска пользователей (показывается только для авторизованных)
- ✅ Условное отображение кнопок на основе данных пользователя
- ✅ AlertDialog для подтверждения логаута

### AppState и синхронизация

- ✅ `AppState` имеет свойство `currentUser` с использованием `mutableStateOf` для обеспечения рекомпозиции
- ✅ `AppState` имеет вычисляемое свойство `isAuthorized` (getter)
- ✅ `AppState` имеет метод `updateCurrentUser()` для безопасного обновления
- ✅ `AppState` имеет свойство `isCurrentRouteTopLevel` для определения корневых экранов
- ✅ `RootScreen` синхронизирует `currentUser` из ProfileViewModel с `appState.currentUser` через `collectAsState()`
- ✅ Кнопка поиска скрывается/появляется мгновенно при logout/login без необходимости навигации

### ProfileViewModel

- ✅ `ProfileViewModel` загружает профиль пользователя, страну и город
- ✅ `ProfileViewModel` подписан на `currentUser` Flow из репозитория
- ✅ `ProfileViewModel` имеет метод `loadProfileFromServer()` для загрузки профиля с сервера после авторизации
- ✅ `ProfileViewModel` создается один раз на уровне RootScreen для предотвращения пересоздания

### Строковые ресурсы

- ✅ Добавлены строковые ресурсы для AlertDialog (`logout_confirmation_title`)
- ✅ Добавлены ресурсы для всех кнопок профиля

### Маршруты навигации

- ✅ Все необходимые маршруты определены в `Screen.kt`:
  - `Screen.MyFriends.route` - для списка друзей
  - `Screen.UserParks.createRoute(userId)` - для добавленных площадок
  - `Screen.UserTrainingParks.createRoute(userId)` - для площадок, где тренируется
  - `Screen.JournalsList.createRoute(userId)` - для дневников
  - `Screen.Blacklist.route` - для черного списка
  - `Screen.EditProfile.route` - для редактирования профиля

### Реализованные экраны

- ✅ `MyFriendsScreen.kt` - экран списка друзей
- ✅ `ParksAddedByUserScreen.kt` - экран добавленных пользователем площадок

### User модель

- ✅ `User` модель содержит все необходимые данные для кнопок:
  - `friendRequestCount` - количество заявок в друзья
  - `friendsCount` - количество друзей
  - `parksCount` (area_count) - количество площадок, где тренируется
  - `addedParks` - список добавленных площадок
  - `journalCount` - количество дневников
  - Вычисляемые свойства: `hasJournals`, `hasFriends`, `hasUsedParks`, `hasAddedParks`

## Требуемые кнопки (из iOS-приложения)

### iOS MainUserProfileScreen содержит

1. **Кнопка "Изменить профиль"** ✅
   - Текущее состояние: кнопка реализована, только логирование
   - Требуется: навигация к `Screen.EditProfile.route`

2. **Кнопка "Друзья"** (с бейджем для заявок в друзья) ✅
   - Текущее состояние: кнопка реализована, навигация работает
   - Навигация: `Screen.MyFriends.route`
   - Экран: `MyFriendsScreen.kt` ✅

3. **Кнопка "Где тренируется"** (показывается если `hasUsedParks`) ✅
   - Текущее состояние: кнопка реализована, только логирование
   - Требуется: навигация к `Screen.UserTrainingParks.createRoute(user.id)`
   - Экран: `UserTrainingParksScreen` ❌ (TODO)

4. **Кнопка "Добавленные площадки"** (динамическое название, показывается если `hasAddedParks`) ✅
   - Текущее состояние: кнопка реализована, навигация работает
   - Навигация: `Screen.UserParks.createRoute(user.id)`
   - Экран: `ParksAddedByUserScreen.kt` ✅

5. **Кнопка "Дневники"** (для главного пользователя всегда показывается) ✅
   - Текущее состояние: кнопка реализована, только логирование
   - Требуется: навигация к `Screen.JournalsList.createRoute(user.id)`
   - Экран: `JournalsListScreen` ❌ (TODO)

6. **Кнопка "Черный список"** (показывается только если есть пользователи в черном списке) ⏸️
   - Текущее состояние: кнопка временно скрыта с TODO
   - Требуется: навигация к `Screen.Blacklist.route`
   - Экран: `BlacklistScreen` ❌ (TODO)

7. **Кнопка "Выйти"** ✅
   - Текущее состояние: кнопка реализована с AlertDialog подтверждением
   - Логаут: выполняется через `appContainer?.logoutUseCase?.invoke()`

8. **Кнопка поиска пользователей** (иконка лупы в топ-баре справа) ✅
   - Текущее состояние: кнопка реализована, только логирование
   - Требуется: навигация к `Screen.UserSearch.route`
   - Экран: `UserSearchScreen` ❌ (TODO)
   - **Важно:** Показывается только для авторизованных пользователей, скрывается для неавторизованных ✅

## План реализации

### Этап 1: UI экрана профиля и AppState (ВЫПОЛНЕНО ✅)

Реализован полный UI экрана профиля с кнопками навигации и AlertDialog для логаута:
- ✅ Вертикальная прокрутка (verticalScroll) и Column с равными отступами
- ✅ Кнопка "Изменить профиль" (SWButton, TINTED)
- ✅ Кнопка "Друзья" с количеством друзей и бейджем заявок (FormRowView) с навигацией
- ✅ Кнопка "Где тренируется" с количеством площадок (FormRowView)
- ✅ Кнопка "Добавленные площадки" с количеством добавленных (FormRowView) с навигацией
- ✅ Кнопка "Дневники" с количеством дневников (FormRowView)
- ✅ Кнопка "Черный список" - временно скрыта (TODO)
- ✅ Кнопка поиска пользователей в топ-баре (иконка лупы)
- ✅ Кнопка "Выйти" с AlertDialog подтверждением
- ✅ Условное отображение кнопок на основе данных пользователя
- ✅ Логирование всех нажатий кнопок в консоль на русском языке
- ✅ AppState с поддержкой авторизации (currentUser, isAuthorized, updateCurrentUser)
- ✅ Синхронизация currentUser между ProfileViewModel и AppState в RootScreen
- ✅ ProfileTopAppBar с условным отображением кнопки поиска

Использованы компоненты дизайн-системы: `UserProfileCardView`, `SWButton`, `FormRowView`.

## Зависимости

**Уже реализовано:**
- ✅ Модель `User` со всеми необходимыми данными
- ✅ `ProfileViewModel` с данными пользователя и Flow подпиской
- ✅ Компоненты дизайн-системы (`SWButton`, `FormRowView`, `UserProfileCardView`)
- ✅ `AppState` с поддержкой авторизации
- ✅ Маршруты навигации в `Screen.kt`

**Требуется для оставшихся задач:**
- ⏳ Реализовать недостающие экраны (EditProfileScreen, UserTrainingParksScreen, JournalsListScreen, BlacklistScreen, UserSearchScreen)
- ⏳ Добавить навигацию к этим экранам в ProfileRootScreen
- ⏳ Данные черного списка (можно добавить заглушку)

## Тестирование

**Текущее состояние:**
- ❌ Нет unit-тестов для UI компонентов ProfileRootScreen
- ✅ Есть instrumented тесты для MyFriendsScreen (MyFriendsScreenTest.kt)
- ✅ Есть instrumented тесты для MoreScreen и ThemeIconScreen

**Рекомендации:**
- Добавить Compose UI тесты для проверки отображения кнопок ProfileRootScreen
- Добавить тесты для проверки логики условного отображения кнопок
- Добавить тесты для проверки работы AlertDialog для логаута

**Примечание:** Тестирование не является обязательным для текущего этапа, но рекомендуется добавить в следующих этапах разработки.

## Критерии завершения

### Этап 1: UI экрана профиля и AppState (ВЫПОЛНЕНО ✅)

- [x] Вертикальная прокрутка, Column с отступами, Spacer для прижимания кнопки выхода
- [x] Все кнопки из iOS-приложения отображаются корректно
- [x] Кнопки показываются условно на основе данных пользователя
- [x] Кнопка "Друзья" показывает бейдж с количеством заявок
- [x] Кнопка "Черный список" скрыта с TODO
- [x] В топ-баре есть кнопка поиска пользователей
- [x] Кнопка поиска скрывается для неавторизованных пользователей
- [x] При нажатии на "Выйти" показывается AlertDialog с подтверждением
- [x] Логаут выполняется только при подтверждении в AlertDialog
- [x] AppState имеет свойства currentUser, isAuthorized, updateCurrentUser
- [x] RootScreen синхронизирует currentUser с AppState
- [x] Кнопка "Друзья" имеет навигацию к MyFriendsScreen
- [x] Кнопка "Добавленные площадки" имеет навигацию к UserParksScreen
- [x] ProfileViewModel создается один раз на уровне RootScreen
- [x] Код отформатирован (`make format`)
- [x] Проект собирается без ошибок

### Этап 2: Реализация оставшихся экранов и навигации (В РАБОТЕ)

- [ ] Кнопка "Изменить профиль" → навигация к EditProfileScreen
- [ ] Кнопка "Где тренируется" → навигация к UserTrainingParksScreen
- [ ] Кнопка "Дневники" → навигация к JournalsListScreen
- [ ] Кнопка "Черный список" → навигация к BlacklistScreen (и убрать TODO)
- [ ] Кнопка поиска пользователей → навигация к UserSearchScreen
- [ ] Реализовать EditProfileScreen
- [ ] Реализовать UserTrainingParksScreen
- [ ] Реализовать JournalsListScreen
- [ ] Реализовать BlacklistScreen
- [ ] Реализовать UserSearchScreen

## Примечания

### Ограничения текущей реализации

1. **Черный список**: Кнопка временно скрыта (TODO). После реализации BlacklistScreen можно добавить отображение.
2. **Кнопки с логированием**: Кнопки "Изменить профиль", "Где тренируется", "Дневники", "Поиск пользователей" пока только логируют нажатия. После реализации соответствующих экранов нужно добавить навигацию.

### Консистентность с iOS

При реализации ориентироваться на iOS-версию:
- Порядок кнопок должен соответствовать iOS-версии
- Стили и отступы должны быть консистентны
- Условное отображение кнопок должно соответствовать логике iOS-версии
- AlertDialog для логаута должен аналогичен iOS confirmationDialog

## Задачи для следующих этапов

### Этап 2: Реализация оставшихся экранов и навигации

**Шаг 2.1: Добавить навигацию к кнопкам в ProfileRootScreen**

- [ ] Кнопка "Изменить профиль" → навигация к `Screen.EditProfile.route`
- [ ] Кнопка "Где тренируется" → навигация к `Screen.UserTrainingParks.createRoute(user.id)`
- [ ] Кнопка "Дневники" → навигация к `Screen.JournalsList.createRoute(user.id)`
- [ ] Кнопка "Черный список" → навигация к `Screen.Blacklist.route` (раскомментировать код)
- [ ] Кнопка поиска пользователей → навигация к `Screen.UserSearch.route`

**Шаг 2.2: Реализовать EditProfileScreen**

- [ ] Создать EditProfileScreen.kt в `ui/screens/profile/`
- [ ] Добавить TopAppBar с кнопкой "Назад"
- [ ] Добавить форму редактирования профиля:
  - Фото профиля
  - Имя
  - Пол
  - Возраст (или дата рождения)
  - Страна/город
  - Описание
- [ ] Добавить кнопку "Сохранить"
- [ ] Добавить в RootScreen навигацию к EditProfileScreen

**Шаг 2.3: Реализовать UserTrainingParksScreen**

- [ ] Создать UserTrainingParksScreen.kt в `ui/screens/profile/`
- [ ] Добавить TopAppBar с кнопкой "Назад" и заголовком "Где тренируется"
- [ ] Показать список площадок, где тренируется пользователь (аналогично ParksAddedByUserScreen)
- [ ] При нажатии на площадку - логировать или навигировать к ParkDetail
- [ ] Добавить в RootScreen навигацию к UserTrainingParksScreen

**Шаг 2.4: Реализовать JournalsListScreen**

- [ ] Создать JournalsListScreen.kt в `ui/screens/profile/` (или `ui/screens/journals/`)
- [ ] Добавить TopAppBar с кнопкой "Назад" и заголовком "Дневники"
- [ ] Показать список дневников пользователя
- [ ] Добавить кнопку "+" для создания нового дневника
- [ ] При нажатии на дневник - навигировать к JournalDetail
- [ ] Добавить в RootScreen навигацию к JournalsListScreen

**Шаг 2.5: Реализовать BlacklistScreen**

- [ ] Создать BlacklistScreen.kt в `ui/screens/profile/`
- [ ] Добавить TopAppBar с кнопкой "Назад" и заголовком "Черный список"
- [ ] Получить список заблокированных пользователей из репозитория
- [ ] Показать список заблокированных пользователей
- [ ] Добавить кнопку "Разблокировать" для каждого пользователя
- [ ] Добавить диалог подтверждения при разблокировке
- [ ] Раскомментировать кнопку "Черный список" в ProfileRootScreen
- [ ] Добавить в RootScreen навигацию к BlacklistScreen

**Шаг 2.6: Реализовать UserSearchScreen**

- [ ] Создать UserSearchScreen.kt в `ui/screens/profile/` (или `ui/screens/messages/`)
- [ ] Добавить TopAppBar с кнопкой "Назад" и полем поиска
- [ ] Добавить поле ввода для поиска пользователей по имени/ID
- [ ] Показать список результатов поиска
- [ ] При нажатии на пользователя - показать профиль или открыть диалог
- [ ] Добавить в RootScreen навигацию к UserSearchScreen

## Рефакторинг

### Архитектурные замечания

Текущий план вызывает `appContainer?.logoutUseCase` прямо из UI (`ProfileRootScreen`). Это соответствует текущему коду в файле, поэтому для текущей задачи это допустимо.

Однако, в идеальной архитектуре (MVVM), метод `logout()` должен находиться в `ProfileViewModel`, и UI должен вызывать `viewModel.logout()`. Это сделало бы код чище и проще для тестирования.

**Рекомендуемый подход (для будущих улучшений):**

```kotlin
// В ProfileViewModel
fun logout() {
    viewModelScope.launch {
        logoutUseCase?.invoke()
    }
}

// В ProfileRootScreen
AlertDialog(
    confirmButton = {
        TextButton(
            onClick = {
                viewModel.logout()
                showLogoutDialog = false
            }
        ) {
            Text(text = stringResource(id = R.string.logout))
        }
    }
)
```

**Преимущества этого подхода:**
- UI не зависит напрямую от `appContainer` и `logoutUseCase`
- Логика логаута инкапсулирована в ViewModel
- Проще тестировать (можно мокать ViewModel вместо всего контейнера)
- Соответствует MVVM принципам (UI → ViewModel → Use Case)

**Примечание:** Этот рефакторинг может быть выполнен в отдельной задаче после завершения текущего этапа.

## История изменений

- 2026-01-31: Создание плана добавления кнопок навигации на экран профиля
- 2026-01-31: Реализация UI экрана профиля с кнопками навигации и AlertDialog
- 2026-02-01: Добавление состояния авторизации в AppState
- 2026-02-07: Обновление плана на основе фактического состояния кода:
  - ✅ Обновлен раздел "Что сделано" с полным описанием реализованного функционала
  - ✅ Обновлен раздел "Требуемые кнопки" с текущим состоянием каждой кнопки
  - ✅ Добавлен раздел "Этап 2: Реализация оставшихся экранов и навигации"
  - ✅ Обновлен раздел "Критерии завершения" с двумя этапами
  - ✅ Убраны дублирующиеся и устаревшие разделы
  - ✅ Добавлены конкретные задачи для реализации оставшихся экранов
