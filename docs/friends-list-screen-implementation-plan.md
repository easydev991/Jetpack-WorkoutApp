# Реализация экрана списка друзей пользователя (MainUserFriendsListScreen)

## Обзор

Создать новый экран для отображения списка друзей и входящих заявок на добавление в друзья для текущего авторизованного пользователя. Экран должен быть аналогичен iOS-версии `MainUserFriendsListScreen.swift`.

### Различие между экранами друзей

**Важно:** В приложении есть два разных экрана для работы с друзьями:

1. **`Screen.Friends`** (из вкладки "Сообщения") - экран выбора друга для начала чата
2. **`Screen.MyFriends`** (из профиля) - экран управления друзьями текущего пользователя (этот план)

### Архитектурный подход: Offline-First с предзагрузкой

**Ключевое решение:** Для обеспечения мгновенного отображения данных на экране `MyFriendsScreen`, используется подход предзагрузки социальных данных в `ProfileViewModel`.

**Как это работает:**
1. `ProfileViewModel` при загрузке профиля вызывает `swRepository.getSocialUpdates(userId)`
2. `getSocialUpdates()` загружает параллельно: пользователя, друзей, заявки, черный список
3. Все данные сохраняются в кэш (UserDao) с флагами: `isFriend`, `isFriendRequest`, `isBlacklisted`
4. При открытии экрана `MyFriendsScreen` данные отображаются мгновенно из кэша

**Преимущества:** мгновенный UX, Offline-First, Single Source of Truth (UserDao), реактивный UI через Flow

---

## Первая итерация ✅ Выполнено

### Этап 0-5: Реализация базового экрана списка друзей

- [x] **ProfileViewModel**: заменен вызов `getUser()` на `getSocialUpdates()` для предзагрузки социальных данных
- [x] **Навигация**: добавлен маршрут `Screen.MyFriends` в `Destinations.kt`
- [x] **Domain Layer**:
  - Создан `FriendsListUiState` (sealed class: Loading, Success, Error)
  - Создан `FriendsListViewModel` с подпиской на UserDao и fallback-механизмом
  - Написаны unit-тесты для ViewModel
- [x] **UI Layer**:
  - Создан экран `MyFriendsScreen` с Scaffold, TopAppBar, LazyColumn
  - Реализованы секции заявок и друзей с логикой скрытия пустых секций
  - Подключена кнопка "Друзья" в профиле
  - Добавлен маршрут в NavHost и factory в AppContainer
  - Обновлена версия БД с 1 на 2
- [x] **Тестирование**: проект собран, форматирование применено, unit-тесты проходят

---

## Вторая итерация: Реализация функциональности заявок в друзья ✅

### Обзор

Реализована функциональность для принятия и отклонения заявок на добавление в друзья с интеграцией сервера и обновлением локального кэша.

### Этап 6-9: Data Layer, ViewModel, UI, Тесты ✅

- [x] **UserDao**: добавлены методы `removeFriendRequest()` и `markAsFriend()` для работы с заявками
- [x] **SWRepository.respondToFriendRequest**: обновлен для синхронизации кэша
  - При принятии: API `acceptFriendRequest` + `markAsFriend` + `removeFriendRequest`
  - При отклонении: API `declineFriendRequest` + `removeFriendRequest`
  - Обновления кэша только при успешном ответе от сервера
  - Обработка IOException и HttpException с логированием на русском
- [x] **ViewModel + UI State**:
  - Добавлено состояние `Busy` в `FriendsListUiState`
  - Добавлено `_lastSuccessState` для сохранения последнего успешного состояния
  - Реализованы методы `onAcceptFriendRequest` и `onDeclineFriendRequest`
  - При состоянии `Busy`: отображение текущих данных с индикатором загрузки поверх
  - Preview функции обновлены
- [x] **Unit-тесты** (ViewModel и Repository):
  - Проверка вызова репозитория с правильными параметрами (`accept = true/false`)
  - Проверка логирования успеха и ошибок
  - Проверка обновления кэша при принятии/отклонении заявки
  - Проверка, что кэш не обновляется при ошибке API

### Дополнительные UI тесты (инструментальные) ✅

- [x] **MyFriendsScreenTest**: 19 тестов, покрывающих:
  - Отображение AppBar с заголовком "Friends"
  - Отображение секций заявок и друзей
  - Отображение кнопок "Принять" и "Отклонить"
  - Вызов callback'ов при нажатии на кнопки и элементы
  - Отображение состояний Loading, Error, Busy, Empty
  - Проверка кликабельности строк
  - Реактивное обновление UI при изменении состояния
- [x] Все UI тесты проходят (100% успех, 63 теста всего, 0 failures)

---

## Этап 10: Тестирование на устройстве

- [x] Собрать проект (`./gradlew assembleDebug`) и запустить приложение
- [x] Выполнить `make format` (ktlint, detekt)
- [x] Запустить unit-тесты (`./gradlew test`) - все тесты для ViewModel и Repository проходят
- [ ] **Проверить на устройстве**:

---

## Этап 11: Доработки UI ✅

- [x] Скрыть Divider после секции заявок, если друзей нет (показывать только при наличии обеих секций)

## Этап 12: Пустое состояние при отсутствии друзей ✅

**Проблема:** При пустом списке друзей (например, после удаления единственного друга) отображается текст "Loading" вместо информативного сообщения.

**Сценарий:**
1. Пользователь открывает экран своих друзей из профиля
2. Открывает профиль единственного друга
3. Удаляет его из друзей
4. Возвращается назад на экран друзей
5. **Ожидается:** Сообщение "Друзей пока нет" / "No friends yet"
6. **Фактически:** Отображается "Loading"

**Решение:**
- [x] Заменить `R.string.loading` на `R.string.no_friends_yet` в пустом состоянии SuccessContent
- [x] Обновить тест `myFriendsScreen_displaysEmptyState_whenNoData`

---
- Переход: Профиль → "Друзья"
- Наличие заявок в секции "Заявки"
- Кнопка "Принять": индикатор загрузки, заявка исчезает, пользователь появляется в "Друзья", реактивное обновление UI
- Кнопка "Отклонить": индикатор загрузки, заявка исчезает, UI возвращается в предыдущее состояние
- Работа без интернета: ошибка логируется, UI не крашится

---

## Критерии приемки второй итерации

- [ ] Функциональность проверена на устройстве
- [x] Код отформатирован (`make format`)
- [x] Проект собирается без ошибок
- [x] Логи на русском языке
- [x] Состояние `Busy` реализовано с индикатором загрузки
- [x] Unit-тесты для ViewModel и Repository проходят
- [x] Инструментальные UI тесты для экрана проходят

**Статус:** Основная функциональность и тесты реализованы ✅, осталось тестирование на устройстве

---

## Заметки для последующих итераций

### Третья итерация (не включена в текущий план)

- Реализовать навигацию на профиль друга при клике
- Добавить обработку ошибок сети с показом Toast/Snackbar
- Добавить refresh/перезагрузку данных на экране друзей
- Добавить Pull-to-refresh

**Четвертая итерация (не включена в текущий план):**
- Добавить анимации
- Улучшить UX (состояния пустых списков, скелетон загрузки)
- Добавить действие удаления друга из списка друзей
- Добавить поиск по списку друзей
