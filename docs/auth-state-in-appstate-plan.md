# План: Добавление состояния авторизации в AppState

## Обзор

Добавить состояние авторизации в `AppState` для удобного доступа к статусу авторизации пользователя из любого экрана приложения. Это позволит мгновенно обновлять UI при изменении статуса авторизации (login/logout) и упростит проверку авторизации на всех экранах (площадки, мероприятия, сообщения и т.д.).

## Назначение

Решить проблему с задержкой обновления UI после logout: кнопка поиска в профиле должна скрываться мгновенно, а не только после навигации на другой экран и возврата. Также предоставить удобный способ проверки авторизации на любом экране без необходимости прокидывать ProfileViewModel через параметры.

## Статус реализации

**Всего этапов:** 6
**Выполнено:** 5 из 6 (83%)
**Осталось:** 1 этап (Тестирование)

### Выполненные этапы ✅

1. ✅ **Этап 1: Модификация AppState** - добавлены свойства `currentUser` (с `mutableStateOf`), `isAuthorized` (getter) и метод `updateCurrentUser()`
2. ✅ **Этап 2: Синхронизация в RootScreen** - реализована подписка через `collectAsState()` и синхронизация через `LaunchedEffect`
3. ✅ **Этап 3: Обновление ProfileTopAppBar** - кнопка поиска использует `appState.isAuthorized`
4. ✅ **Этап 4: Примеры использования** - добавлен пример в `ParksTopAppBar` и `CreateParkFab`
5. ✅ **Этап 5: Обновление документации** - обновлен `profile-buttons-enhancement-plan.md` и создан `docs/appstate-auth-state.md`

### Невыполненные этапы ❌

6. ❌ **Этап 6: Тестирование** - не выполнено ручное тестирование функциональности и не созданы unit-тесты

### Примечания о реализации

- Функциональность полностью реализована в коде
- Все этапы разработки (код) завершены
- Документация обновлена
- Тестирование не проводилось (ручное или автоматическое)
- Код отформатирован и собирается без ошибок

## Текущая проблема

- `RootScreen` читает `profileViewModel.currentUser.value` один раз при отрисовке
- При logout значение в ViewModel меняется на `null`, но `RootScreen` не узнает об этом
- Кнопка поиска скрывается только после навигации на другой экран и возврата
- На других экранах нет удобного способа проверить авторизацию (нет доступа к ProfileViewModel)

## Критически важное замечание о Compose

**ВАЖНО:** Обычные переменные (`var`) в Jetpack Compose **НЕ вызывают рекомпозицию**. Если использовать обычные `var` в `AppState`, то UI не будет обновляться при изменении состояния авторизации!

### Почему это происходит

- **Обычные `var`** - обычные переменные Kotlin, Compose не "видит" их изменения
- **`mutableStateOf`** - создает отслеживаемое состояние, которое запускает рекомпозицию при изменении

### Решение

Обязательно используйте `mutableStateOf` для свойств `AppState`, которые должны вызывать обновление UI. Это критически важно для мгновенного обновления кнопок и других элементов, зависящих от авторизации.

## План реализации

### Этап 1: Модификация AppState (ВЫПОЛНЕНО ✅)

**Файл:** `app/src/main/java/com/swparks/navigation/AppState.kt`

- [x] Добавлены импорты `User` модели и `setValue` для работы с mutableState
- [x] Добавлено свойство `currentUser: User?` с использованием `mutableStateOf` (private set)
- [x] Добавлено вычисляемое свойство `isAuthorized: Boolean` (getter)
- [x] Добавлен метод `updateCurrentUser(user: User?)` для безопасного обновления

**Реализованный код:**

```kotlin
// ==================== Состояние авторизации ====================

var currentUser by mutableStateOf<User?>(null)
    private set

val isAuthorized: Boolean
    get() = currentUser != null

fun updateCurrentUser(user: User?) {
    currentUser = user
}
```

**Критерий завершения:** ✅ все выполнены

---

### Этап 2: Синхронизация в RootScreen (ВЫПОЛНЕНО ✅)

**Файл:** `app/src/main/java/com/swparks/ui/screens/RootScreen.kt`

- [x] Добавлен импорт `LaunchedEffect` и `collectAsState`
- [x] Подписка на `profileViewModel.currentUser` через `collectAsState()`
- [x] Синхронизация через `LaunchedEffect` с вызовом `appState.updateCurrentUser()`

**Реализованный код:**

```kotlin
val currentUser by profileViewModel.currentUser.collectAsState()

LaunchedEffect(currentUser) {
    appState.updateCurrentUser(currentUser)
}
```

**Критерий завершения:** ✅ все выполнены

---

### Этап 3: Обновление ProfileTopAppBar (ВЫПОЛНЕНО ✅)

**Файл:** `app/src/main/java/com/swparks/ui/screens/profile/ProfileRootScreen.kt`

- [x] Параметр `showSearchButton: Boolean` заменен на `appState: AppState`
- [x] Кнопка поиска использует условие `if (appState.isAuthorized)`

**Реализованный код:**

```kotlin
@Composable
fun ProfileTopAppBar(
    appState: AppState,
    onSearchUsersClick: () -> Unit = {}
) {
    CenterAlignedTopAppBar(
        title = { Text(text = stringResource(id = R.string.profile)) },
        actions = {
            if (appState.isAuthorized) {
                IconButton(onClick = onSearchUsersClick) {
                    Icon(Icons.Default.Search, contentDescription = stringResource(id = R.string.profile))
                }
            }
        }
    )
}
```

**Файл:** `app/src/main/java/com/swparks/ui/screens/RootScreen.kt`

- [x] Вызов `ProfileTopAppBar` обновлен с передачей `appState`

**Критерий завершения:** ✅ все выполнены

---

### Этап 4: Примеры использования на других экранах (ВЫПОЛНЕНО ✅)

**Файл:** `app/src/main/java/com/swparks/ui/screens/parks/ParksRootScreen.kt`

- [x] `ParksTopAppBar` обновлен с параметром `appState`
- [x] Добавлен компонент `CreateParkFab` как пример использования `appState.isAuthorized`
- [x] Добавлено логирование на русском языке

**Реализованный код:**

```kotlin
@Composable
fun CreateParkFab(
    appState: AppState,
    onClick: () -> Unit = {}
) {
    if (appState.isAuthorized) {
        FloatingActionButton(onClick = {
            Log.i("ParksScreen", "Нажата кнопка создания площадки")
            onClick()
        }) {
            Icon(Icons.Default.Add, contentDescription = "Создать площадку")
        }
    }
}
```

**Файл:** `app/src/main/java/com/swparks/ui/screens/RootScreen.kt`

- [x] Вызов `ParksTopAppBar` обновлен с передачей `appState`

**Критерий завершения:** ✅ все выполнены

---

### Этап 5: Обновление документации (ВЫПОЛНЕНО ✅)

**Файлы:**
- `docs/profile-buttons-enhancement-plan.md` - обновлен с описанием использования AppState
- `docs/appstate-auth-state.md` - создан новый файл с документацией

- [x] Обновлен `profile-buttons-enhancement-plan.md` с примечаниями и историей изменений
- [x] Создан файл `docs/appstate-auth-state.md` с полной документацией:
  - Описание назначения AppState
  - Свойства авторизации (`currentUser`, `isAuthorized`, `updateCurrentUser`)
  - Примеры использования на разных экранах
  - Объяснение синхронизации через `LaunchedEffect`
  - Важные примечания о мгновенном обновлении UI
  - Типичные сценарии использования

**Критерий завершения:** ✅ все выполнены

---

### Этап 6: Тестирование (НЕ ВЫПОЛНЕНО ❌)

#### 6.1 Проверка функциональности

**Ручное тестирование (на устройстве/эмуляторе):**

- [ ] Тест 1: Открыть приложение, убедиться что `appState.currentUser` = `null` и `appState.isAuthorized` = `false`
- [ ] Тест 2: Авторизоваться, убедиться что `appState.currentUser` содержит данные пользователя и `appState.isAuthorized` = `true`
- [ ] Тест 3: Проверить что кнопка поиска в профиле появилась сразу после авторизации (без навигации)
- [ ] Тест 4: Выполнить logout через кнопку "Выйти" в профиле
- [ ] Тест 5: Проверить что кнопка поиска в профиле исчезла сразу после logout (без навигации)
- [ ] Тест 6: Перейти на экран "Площадки" и убедиться что `appState.isAuthorized` доступен
- [ ] Тест 7: Повторно авторизоваться и убедиться что состояние обновилось корректно

**Примечание:** Unit-тесты для AppState не создавались. В соответствии с правилами TDD плана, тесты должны быть написаны ДО реализации, но на текущий момент фаза тестирования не выполнена.

**Критерий завершения:**
- [ ] Все ручные тесты пройдены успешно
- [ ] Кнопка поиска скрывается/появляется мгновенно при logout/login

---

## Критерии завершения реализации

### Функциональность

- [x] AppState имеет свойство `currentUser` с использованием `mutableStateOf`
- [x] AppState имеет вычисляемое свойство `isAuthorized` (getter)
- [x] AppState имеет метод `updateCurrentUser()` для безопасного обновления
- [x] RootScreen синхронизирует `currentUser` из ProfileViewModel с `appState.currentUser` через `updateCurrentUser()`
- [x] Синхронизация использует `collectAsState()` для реактивного обновления
- [x] ProfileTopAppBar использует `appState.isAuthorized` для условного отображения кнопки поиска
- [x] Добавлен пример использования `AppState` в `ParksTopAppBar` и `CreateParkFab`
- [ ] Кнопка поиска скрывается мгновенно при logout (требуется ручное тестирование)
- [ ] Кнопка поиска появляется мгновенно при login (требуется ручное тестирование)

### Качество кода

- [x] Код отформатирован (`make format`)
- [x] Проект собирается без ошибок (`./gradlew assembleDebug`)
- [ ] Нет новых ошибок линтера (требуется проверка)
- [x] Не используются устаревшие API

### Документация

- [x] `profile-buttons-enhancement-plan.md` обновлен с описанием использования AppState
- [x] Создана документация `docs/appstate-auth-state.md`
- [x] Примеры использования добавлены в документацию
- [x] История изменений обновлена в `profile-buttons-enhancement-plan.md`

### Тестирование

- [ ] Unit-тесты для AppState не созданы (TDD не выполнен)
- [ ] Ручное тестирование авторизации/логаута с мгновенным обновлением UI
- [ ] Проверка доступности `appState.isAuthorized` на разных экранах

---

## Примечания о реализации

### Преимущества решения

1. **Мгновенное обновление UI**: Использование `collectAsState()` гарантирует реактивное обновление при изменении `currentUser`
2. **Доступность везде**: `AppState` уже используется во всех экранах, не нужно прокидывать дополнительные зависимости
3. **Соответствие архитектуре**: Следует MVVM без введения глобального состояния через CompositionLocal
4. **Масштабируемость**: В `AppState` можно добавить другие глобальные состояния (тема, язык, фильтры)
5. **Легко тестируется**: AppState можно создать вручную в тестах

### Технический долг

1. **Отсутствие unit-тестов**: В соответствии с правилами TDD, тесты должны быть написаны ДО реализации, но на текущий момент тесты для AppState отсутствуют
2. **Не проведено ручное тестирование**: Функциональность не была проверена на устройстве/эмуляторе
3. **Не выполнена проверка линтера**: Требуется запустить `make lint` для проверки на новые ошибки

### Следующие шаги (вне этого плана)

- **Приоритет 1 (тестирование):** Провести ручное тестирование на устройстве/эмуляторе для проверки мгновенного обновления UI при login/logout
- **Приоритет 1 (тестирование):** Создать unit-тесты для `AppState` и проверки синхронизации в `RootScreen`
- Использовать `appState.isAuthorized` для условного отображения FAB кнопок на экранах площадок/мероприятий
- Добавить проверки авторизации в MessagesScreen для показа/скрытия чатов
- Реализовать навигацию на экран авторизации при попытке доступа к закрытым функциям для неавторизованных пользователей
- Рассмотреть добавление других глобальных состояний в `AppState` (тема, язык, предпочтения)

---

## Консистентность с iOS

В iOS-приложении аналогичная функциональность реализована через `@EnvironmentObject` для DefaultsService. AppState в Android выполняет ту же роль - глобальное состояние, доступное из любого экрана UI.
